# Tennin 언어 배우기

언어 이름이 Tennin 인 이유는 일본 그거가 아니라 그냥 Tennis 에서 따왔다. 이름 뭘로 지을지 생각할때 테니스 치고 있어서

killer use case 먼저 봐보자

## killer use case

**학습 곡선을 포기한게 왜 포기 했나**: 애초에 이 언어의 목적이다. 어떻게 보면 킬러 유스케이스중 하나다.\
원래 다른 언어들은 한가지 문제가 있다. 코드가 많이 복잡하다. if 문은 {} 가 블럭인데 또 .forEach 로 배열 순회는 () => {} 가 블럭처럼 쓰인다.\
그리고 애초에 이 조건이 충족하면 이걸 실행해라 할때 그냥

```
this.runIf (1 == 1) {
   print("Hello, World!");
}
```

이렇게 하면 않되고 복잡하게

```
let condition: condition = {
   values: [pointEnum.ch, "~"],
   opeartors: ["same"],
};
this.runIf(condition, () => {
   print("Hello, World");
});
```

를 해야한다. 애초에 이건 형식 따윈 개나줘버리고 언어가 허용하지 않는걸 다른 방식으로 우회해 코드가 더 복잡해 진다. 이런 코드를 짜는 언어는 누가 좋아하겠냐

Java 같은 언어는 그래도 차라리 괜찮지만 그래도 아직 저런 방식의 코드를 짜야한다. Java 는 그냥 괄호 몇개 사라지고, 키워드가 다 정령되어 보이는것 뿐이다.

Tennin 은 이걸 해결하고 이런 코드가 가능하다.

```tennin
this.runIf (this.ch == "~"):{
   Line.addLine("Hello, World!");
};
```

훨씬더 보기 좋다. if 문도

```tennin
if (1 == 1):{
   Line.addLine("Hello, World!");
};
```

일관성까지 두마리의 토끼를 잡았다.

그대신 명백한 트레이닝 오프가 있다. 학습하기가 너무 어렵다. 진짜 어렵다. 그대신 코드가 훨신더 깔끔해진다. 만약 코딩 입문자라면 이 문서는 바로 때려 치우고 Python 부터 배우기 쉬운 언어를 공부하기를 바란다.

---

그리고 Lisp 의 방언 문제를 지적할수도 있다. 이 부분은 이 Tennin 의 문법을 전체 이해하고 나서 봐야지 이해가 될텐데 그래도 대충은 봐보는걸 추천한다. 이게 무슨 언어고 왜 필요하고 장점 이걸 써야하는 이유가 무엇인지

Lisp 는 프로젝트가 방언을 선택한다. 하지만 Tennin 은 프로젝트가 자신에 맞는 프레임워크 그러니까 방언을 만들고 그걸로 프로젝트를 만든다. 둘이 비슷해 보이지만 아예 다르다 Lisp 는 방언을 만든다. 그런데 프로젝트는 자신에 맞는 방언을 선택해야 한다.\
단점은 다른 프로젝트를 가면 이게 뭔뜻인지 **못알아 듣는다.**

그런데 Tennin 은 다르다. 다른 프로젝트에 가도 형식은 유지되고 문법이 유지되기 때문에 무슨 뜻인지는 함수가 뭐하는진 몰라도 **알아들을순 있다.** 왜냐하면 모든 것이 다 나라와 소국, 인자함수로 통일되기 때문에 그걸로 다른 프로젝트에 방언이나 프레임워크라고 할수 있는걸\
Lisp 와 다르게 대충은 **알아들을순 있다.**

이게 쫌 어려운데 예시를 들어보자. Lisp 의 common lisp 는 매크로로 아예 OOP 까지도 **구현해 냈다.** 하지만 단점이 크다. 이 방언은 다른 Lisp 방언 사용자가 이게 무슨 뜻인지 **못알아 듣는다.**

그런데 Tennin 코드를 보자, 프로젝트마다 아예 새로운 방언을 쓰고 그 방언 프레임 워크를 쓰더라도 뭐든지 똑같이 `self.runIf ():{}` 이것도 어떻게 보면 여기서 () 가 조건이고 {} 가 블럭이 올꺼라는걸 FuncKind, 인자함수의 형태로 **알수가 있다.** 새 개념이 아니라 **기존 개념이다**.

아직도 이해가 잘 되지 않는다면 그냥 공식 문법 문서 전체를 보길 바란다. 이 개념은 오직 이걸 이해 해야지만 이해할수 있는 개념이다.

그래도 마지막으로 비유로

Lisp 는 방언 수준이 아니라 다른 언어 수준이다. *영어, 한국어, 일본어* 그런데 그 둘을 모두 언어라는 형식만 있지 사실은 많이 **달라 보인다**. 이게 Lisp 의 방언이다.

Tennin 은 사투리다. 프로젝트 그러니까 지역마다 이곳에 맞게 발전해 왔고, 어쨋든 제주도 사투리급만 아니면 다른 지역에 사투리를 **알아들을수가 있다.**

근데 이래도 물을수 있다. 아직도 방언 문제가 남아 있다고, 이걸 어떻게 해결할꺼냐고

오히려 **정 반대다** 반언 문제는 없고 다른 언어들의 방언 문제를 해결했다. 이게 무슨 말일까?

다른 언어 Python 의 NumPy 를 봐라, 그냥 다른 언어다 처음보면 알수가 없는 `a[:, None, ::2]`, 아예 이게 뭔지도 모르겟는 `a[a > 3]` 이게 방언이다. 이렇게 방언이 못생겨진 이유는 언어가 이 부분은 어쩔수 없이 우회를 하게 하기 때문이다. Python 은 자기만의 블럭을 만들려면 우회를 쫌 많이 해야한다. 새 함수를 만들고 callback 부분에 그걸 넣고, 그래도 lambda 라도 있어서 다행이지 그것까지 없었다면... 끔찍하다.

그리고 JavaScript, TypeScript 에서도 우회는 계속 된다. 컴파일러에서 이 조건이면 건너 뛰기 이런걸 할때 식을 대입하면 그거의 결과 그러니까 `this.ch == "~"` 이러면 식 자체가 전달되지가 않고 true, false 가 전달된다. 다른 언어들의 약점이다.

그래서 다른 언어들은 이걸 또 우회하기 위해서 `{values: [thisObjectEnum.ch, "~"], operators: ["same"]}` 이런 자료 구조가 탄생한다. *WOW!!!* 정말 보기가 힘들다. 그래서 유지보수를 위해서 "this.ch == "~" 를 저 형식으로 봐꾸는 코드를 만들고 또 그거를 위해 파서, 토크니저를 만들고... 이 단순한거 하나때문에 프로그래밍 언어를 하나 만든다. *WOW!!!* 정말 말도 않된다. 이걸 왜 그냥 `this.ch == "~"` 이걸 그냥 전달할수가 없는건가, 참고로 이게 Tennin 의 IOP 등장에 핵심 배경이다.

그리고 NumPy 에 이상한 기호들의 방언 문제는 Tennin 은 그걸 의미가 보이게 한다. 저걸 그냥 처음 보면 무슨 뜻인지 알수가 없다, 왜냐면 저건 Python 이 저런걸 지원하지 않아 **우회 했기 떄문이다.** 그런데 Tennin 은 우회할 이유를 안만들고 우회할 필요를 없게 만든다. 코드는 저런 이상한 ::2, : 이상한 기호들 이해가 않되는 기호들에서 진짜 언어, 저런게 직접 단어로 변하게 된다.

(이거 작성할때 NumPy 문법을 몰라서 저거의 뜻을 최대한 해석해 볼려고 했지만 도저히 저거 `a[:, None, ::2]` 이런게 뭔뜻인지 해석이 않되서 직접 Tennin 에서 활용 예시를 못만들었 습니다. 죄송합니다.)

그러면 물을수 있다. **애초에 다른 언어는 한가지 형식을 유지하는데 Tennin 은 인자함수라는 새 재어구조와 expand 라는 매크로 때문에 방언이 일반적인 언어에서는 안생기는 데에서도 방언이 생기지 않냐**

아니다. Tennin 의 함수에서 인자함수나 나라도 사실 언제 써야 하냐를 명확히 알면 그냥 진짜 이 함수의 선언 문을 몰라도 대충 이함수에 나라, 나라에 몇번째 나라인지, 그런것만 알아도 함수를 쓸수가 있다.

먼저 이 함수에 뭐 언제 국을 쓰냐, Callback 이 필요할때다. 그런데 다른 언어들은 callback 이 복잡하다. 뭐 어디선 이 몇번째 파라미터에 무슨 callback 을 넣야하고 또 if 문이 {} 을 callback 처럼 사용하는데 사용자는 그걸 못만들고 그걸 구현하기 위해서 뭐... 그리고 결과를 보면 정말 놀랍다. **WOW!!!** goto 가 남발하고 while 이 몇겹이 있고 또 그냥 코드를 보면 저게 뭐하는거인지 감도 안오는 코드가 완성된다! 예시로 [예시 프로젝트](https://www.chiark.greenend.org.uk/~sgtatham/mp/) 에 가보면 이 사람이 그 자기만에 Loop 를 위해 얼마나 고생했는지 보인다.

그래자기고 이 Tennin 의 callback 나라 규칙은 이거다. 한 묶음은 나라로 묶어서 보관한다. 그러니까 이걸 만드는 코드가 무었인지 doing 이라는 나라로 보관하는 그런식이다. 그리고 애초에 그냥 나라가 뭐있는지 고민하는거랑 callback 이게 몇번째 순서인지 고민하는거랑 같다.

그리고 또 애초에 새 프로젝트를 봤는데 그 함수들이 처음부터 쓸수 있고 이상한 외계어로 안들리는게 더 이상한거다. 그냥 이상한 외계어로 들리는건 모든 언어가 그렇다. 그냥 코드를 이해 해야지 저걸 쓸수가 있는거다. 그냥 이게 싫으면 AI 한테 바이브 코딩하면 된다. 그리고 애초에 Tennin 의 국, 소국 시스템도 처음엔 외계어로 보일꺼다. 하지만 더 명확하다. callback 을 직접 인자함수라는 블럭처럼 사용해 이 함수를 사용하고 이게 오히려 그냥 함수에 인자를 넣는다라는 형식만 유지하며 callback 을 함수로 굳이 넣는것보다 더 보기 쉽다. 익히기도 더 쉽다. 왜냐면 형식이 명확하다. callback 은 언제나 {} 또는 ():{} 이렇게 간다. 진짜 명확하다. 오히려 이 callback 부분은 익히기가 더 쉬운거다.

또한 expand 는 그냥 메크로랑 비슷한거다. 또한 Lisp 처럼 **제어 구조까지 봐꿀수가 없다**. Lisp 는 매크로 아예 다른 언어를 만들수가 있다. 하지만 Tennin 은 그건 않된다. 왜냐면 expand 는 이제 코드를 그 환경 거기에 환경에 맞춰 실행을 해야할때 쓰는거고 그걸로 뭐 인자함수를 다른 형태로 사용한다던가 함수를 다른 형태로 사용하는건 절대로 하지 못한다. expand 는 그냥 아예 다른 방식으로 코드를 짜는걸 지원해주는것이 아니라 그냥 그 코드를 그 사용하는쪽 환경에 맞춰 실행할때 사용하는거다.

## 대상 사용자

**누굴 위한 언어냐.**

Tennin 은 갓 처음 코딩을 시작해서 스크래치나 하고 있는 어린이, 처음 코딩을 하는 사람들이 대상이 아니다. 숙력된 파워 유저가 대상이다.

**트레이닝 오프**

트레이닝 오프가 명백하고 크다. 너무 어렵다. 하지만 **이 문서에선 처음 코딩 하는 사람도 최대한 쉽게 Tennin 을 배울수 있게 해볼것이다.**

개념은 **비유도 넣고** 또 최대한 일반인도 알아들을수 있는 걸로 설명하고 왜 필요한지, 이게 왜 필요한지도 넣을것이다.

그다음으로 철학을 보자, 철학은 이 언어에 대한 정체성이다. 코딩 처음 하는 사람, 블럭 코딩만 하는 사람은 철학은 건너 뛰어라, 애초에 뭔 소리인지 못알아 듣는다.

## 설계 철학 (구현 원칙)

> Core: 구현 복잡도를 낮추는건 다른 어떤것보다 중요하다.

Tennin 자체를 만들 때 지키는 원칙.

1. **성능이 최우선이다.** 노이즈 수준의 성능 낭비도 허용하지 않는다. 안그러면 성능을 위해 이상한 코드가 생긴다.
2. **가독성이 중요하다.** 가독성이 않좋으면 이상한 코드가 이상해 보인다.
3. **메모리를 최대한 절약한다.** 안그러면 메모리 절약을 위해 이상한 보기 싫은 뭔소리인지 못알겠는 코드가 탄생한다.
4. **Deep Module을 쓴다.** (좁은 인터페이스, 깊은 구현)
5. **사소한 이유로 새 추상화를 만들지 않는다.** 래퍼(wrapper) 같은 얕은
   간접층은 메모리·성능 낭비다.
6. **불필요한 간접층을 만들 빌미 자체를 제공하지 않는다.** 언어 설계가
   래퍼를 만들 이유를 만들어서는 안 된다.
7. **Jump는 최소화 해야 한다.** Jump 는 읽을 때의 흐름을 끊고 가독성을 낮춘다.
8. **파일간 Jump는 거의 없애야 한다.** 파일간 Jump 는 작업을 힘들게 한다.
9. **사용자와 시스템을 구분하지 말라.** 내장 함수 쓰는건 System.print(); 이런 식이고
   자기가 만든 함수 쓰는건 User.print(); 이런 식으로 하지 말라. 오히려 자유도를 아주 낮춘다.
10. **절대로 똑같은 기능을 여러개 넣지 말라.** 비슷한 기능 그러니까 예를 들어 키워드 추가 기능과 함수 기능
   이건 아무런 차이가 없다. 그냥 언어를 복잡하게 할 뿐이다.
   그리고 비슷한 기능은 표현지향을 사용해 구분한다.
11. **기능을 추가할 때 중복이 생기면 기능을 지우지 말고 중복이 생긴 여지를 없애라.** 기능이 추가될 때
   만약 다른 것과 겹친다면 예: `//` 를 주석으로 하려고 했는데 나눗셈의 몫을 반환하는 연산자와 겹칠 때
   기능을 제거하지 말고 아예 중복이 생길 이유 자체를 없애라 예: `//` 주석 기호가 다른 것과 겹치면 기능 제거 대신에 `@...` 을 주석으로 한다.
12. **IDE 이 가독성을 높여주지 않는다.** 문법 자체가 가독성이 높아야 한다.

    (0xd 는 불길한 숫자라 건너뛴다 — 오타가 아니다.)

14. **언어가 특정 메커니즘을 강제하지 않는다.** 메모리 관리 방식, 에러 처리 방식 등은 언어가 하나
   로 못 박지 않고 사용자가 고른다. 강제된 메커니즘은 그것을 원하지 않는 사용자의 자유도를
   빼앗는다.
15. **언어는 유연해야 한다.** 언어는 원래 많은걸 더 쉽게 표현할 수 있어야 한다. 만약 유연하지 않으면
   가독성이 낮아진다 아주 많이 이 언어가 지원하지 않는걸 구현하기 위해.
16. **언어 컴파일러에 /\s/ 는 건너 뛴다고 되어 있어야 한다.** 애초에 들여쓰기로 구분하면 python 처럼 가독성을 올라가겠지만
   공백이 제대로 보이지 않는 markdown, gpt 같은 곳에서는 뜻이 제대로 전달되지 않을 수도 있다.
17. **안전해 보이는 코드는 안전성이 보장되고, 위험한 건 위험해 보인다.** 먼저 위험한게 안전해 보이는건 최악의 상황이다.
   위험한데 안전해 보여서 그걸 좀 실수로 잘못 바꿔서 큰일이 터질 수도 있다.
   그렇다고 좀 위험한 작업까지 다 막는건 안 좋다. 왜냐면 그러면 언어로 못하는 작업이 너무 많아 진다.
   그래서 그 두 문제의 해결책은 안전해 보이는 코드는 안전이 보장되고, 위험한건 위험해 보인다 이다.
   왜냐면 안전해 보이는건 안전이 보장되면 확실히 프로그래머는 안심하고 안전해 보이는 구간은 코드를 짤 수 있다.
   또한 위험해 보이는건 프로그래머가 직접 긴장하면서 코드를 짜게 되고 그러면 많은걸 하면서 안전하게 코드를 짤 수 있다.

또한 Tennin 는 표현 지향 Representation-Oriented-Programming ROP 과 정보 지향 Information-Oriented-Programming IOP 언어다. 무슨 뜻이냐면

먼저 표현 지향은 간단히 무엇이든지 option 으로 받지 않고 표현으로 받는것이다. 예를 들어서 이게 반복문인지를 받지 않고 그냥 이거의 종류가 반복문이라는걸 받는다.\
그러니까 그런거다. 옵션에서 선택하는 방식 그러니까 스무고개 이건 ; 으로 구분 하나요?, 이건 블럭이 {} 인가요 이런 식으로 받지 말고 그냥 사용자가 표현을 하게 한다.\
이게 표현 지향이다. 왜 있냐면 단순히 Option 으로 받으면 자기만의 새로운 구조를 만들지 못한다. 전혀, 하지만 표현지향은 자신만의 새로운 구조를 만들수 있게 한다. 슬롯에 특이한 방식으로 값을 받는걸 넣거나\
아예 이세상에는 없는 자료 구조를 만들어 벌리수도 있다. 이게 표현지향이다. 이게 표현지향인지 구분하는법은 여기서 새로운 구조를 새로 만들수 있냐다.

표현지향의 예시다.

```
for (
   int i;
   start = 10;
   end = 20;
) {
   ...
}
```

이건 직접 얼마나 반복할지 표현하고, 말한다.

이번엔 ROP 의 반대 OPOP (Option Oriented Programming) 을 보자 (참고로 Option Oriented Programming 이 OOP 가 아니라\
OPOP 인 이유는 OOP 라고 하면 Object Oriented Programming 과 착각할수도 있기 때문이다. )

```
for (
   variableNameIsA = false;
   variableNameIsB = false;
   variableNameIsC = false;
   variableNameIsD = false;
   variableNameIsE = false;
   ...
   variableNameIsi = true;
   startIsOne = false;
   startIsTwo = false;
   startIsThree = false;
   startIsFour = false;
   startIsFive = false;
   ...
   startIsTen = true;
   endIsOne = false;
   endIsTwo = false;
   endIsThree = false;
   endIsFour = false;
   endIsFive = false;
   ...
   endIsTwenty = true;
) {
   ...
}
```

이게 OPOP 다

(\
   참고로 OPOP 가 단점만 있는건 아니다. 매개변수를 받는걸 isArgs = true; 말고 직접 파싱하게 하면\
   너무 사용자가 쓰기 어렵다.\
   그래서 뭐 사용자가 저런걸 다 하게 하기 싫다면 OPOP 를 쓰는것도 방법이다.\
   하지만 Tennin 은 극적 ROP 고 OPOP 는 사용하지 않는다.\
)

정보지향은 무엇이 전달될 때 필요한 정보는 모두 보존한다. 그러니까 예를 들어서 일반적인 언어는 함수를 callback 으로 받을 때 이름은 안 받고 그냥 함수 자체를 받음 그 대신 정보지향에서는\
이 함수의 이름이 뭔지, 어디서 호출이 됐는지, 이 호출된 위치가 어딘지 그런것까지 모두 기록해서 준다.\
정확히는 그냥 정보가 보존되는거다. 함수 이름이나, 함수가 어디에서 쓰이나 이런것까지 모두 보존하는 그런 프로그래밍 방식이\
정보지향이다.

그러면 궁금할 수 있다 — 정보 지향은 메모리 낭비가 아니냐. Tennin 의 답은 **정보 굽기**다: 정보는 나르지 않고, 굽는다.

정보 지향은 두 층이다.

- **정적 층 (대부분):** 함수·값의 정보 — 이름, 선언 위치, 호출 위치, kind, 스펙 — 는 컴파일 타임에 전부 알려져 있다. 코드가 실제로 접근하는 조각만 expand 가 그 자리에 상수로 굽는다. 예를 들어 `f.name` 을 쓰면 그 자리에 이름 문자열 상수가 박히고, 접근하지 않는 정보는 실행 파일에 아예 존재하지 않는다 — 메모리 절약이 아니라 메모리 0 이다. 호출 위치처럼 호출마다 다른 정보는 expand 가 호출 지점에서 끼워 넣는다.
- **동적 층 (드묾):** 임의의 값들을 돌면서 정보를 묻는 진짜 동적 질의만, 타입마다 하나 있는 **정보표**를 참조한다. 표는 프로그램에 하나뿐이라 값마다 복사가 없고, 이 비용은 동적 질의를 쓴 코드만 낸다 (비용 정직).

그리고 "정보 지향을 쓸 거면 미리 위에 표시하면 되지 않냐"고 물을 수 있는데, Tennin 는 점프를 싫어한다. 이건 그냥 지어낸 게 아니라 python 에서 직접 import 를 했을 때 코드 흐름을 깨고 맨 위로 가서 import 를 추가하는 게 정말 귀찮았기 때문이다. 정보 굽기에서는 표시 자체가 필요 없다 — 그냥 접근하면 컴파일러가 굽는다. 위로 올라갈 일이 없다.

(이전 설계의 정보 지향 큐·val box 는 폐기한다 — 값을 넘길 때마다 큐를 만드는 비용과 요청-응답 지연이 사라진다. → CHANGELOG. 영감: Zig 의 comptime 리플렉션 계보에서 얻었다. 단, "사용하는 조각만 굽기 + 호출부 주입 + 선택적 정보표"라는 조합은 Tennin 의 expand·두 층 원칙에서 도출한 것이다.)

**`expression` 타입 — 수식을 보존해서 받기.** 정보 지향이 매개변수 층위에서 드러나는 대표적 표면이다. 보통 언어는 `1 + 2 == 3` 을 매개변수로 받으면 그냥 boolean(`true`/`false`) 하나로 뭉개진다. Tennin 에서 매개변수 타입을 `expression` 으로 잡으면 **수식 자체가 정보를 보존한 채로** 온다.

- `.calculation` — 계산 과정. 예: `1 + 2 == 3` → `[1, operator.plus, 2]`
- `.result` — 결과값. 예: `3`

이 덕에 `change`(거스름돈 — 어떤 수식이 표현한 값이 실제 값과 얼마나 다른지 알려주는 함수) 같은 걸 만들 때, 연산자를 `"add"`·`"star"`·`"divide"` 같은 문자열로 받는 복잡한 우회 없이 그냥 `1 + 2 == 3` 을 자연스럽게 넘길 수 있다.

- **왜:** 연산자를 문자열로 받게 하면 표현도 어렵고 가독성도 나쁘다. 수식을 값으로 보존해서 받으면 `change` 류(단언·심볼릭 계산·분석 도구)를 훨씬 유연하게 만들 수 있다. **비용은 옵트인** — `expression` 으로 받을 때만 보존 비용이 들고, 기본 boolean 경로의 성능은 해치지 않는다(비용 정직).
- **선례:** C# 은 람다를 `Expression` 타입에 대입하면 코드 대신 식 트리가 잡히고 LINQ 가 그 위에 선다. 타입 선언만으로 정해지는 것이 Tennin 식이다.

> 요약: 언어가 좋음이 컴파일러 말고 언어 문법에서부터 나와야 한다.

## 1-B. 실행 스타일 (언어 동작 원칙)

Tennin 언어가 사용자에게 보이는 동작 방식.


1. **콜백은 함수로만 전달한다.**
2. **제어 구조도 콜백이다.** `if`, `while` 같은 것도 특수 문법이 아니라
   함수에 넘기는 콜백(클로저)으로 다룬다. → 사용자가 자기만의 제어 구조를
   함수로 정의할 수 있다.
3. **이름은 겉모습일 뿐, 기능을 담당하지 않는다.** 이름 기반 매직(특정
   이름이 자동으로 특별하게 동작하는 것)을 금지한다. 기능은 명시적 구조로만
   결정된다.
4. **구현 자유도가 중요하다.** 자유도를 위해서라면 낮은 진입 장벽을 포기해도 된다.
5. **사용자가 언어에 기능을 추가할 수 있을 만큼의 자유도를 준다.**
6. **파일은 Yulist 일 수도 있고, 함수 일 수도 있고, 다른 변수 등일 수도 있다.** 파일은 모든 형태가 가능하다
   파일은 Yulist(타입) 일 수도 있고, 함수로 만들 수도 있다. (자유도를 높이기 위해)
7. **함수는 1급 시민이다.** 함수가 1급 시민이 아니면 자유도가 많이 떨어진다.
   그러므로 함수는 1급 시민이다.
8. **이상한 축약어는 쓰지 않는다.** 이상한 축약어는 발음도 이상해서 정보 전달이 어렵다.
   또한 다른 축약어로 오해를 일으킨다.
   하지만 i 를 index 말고 ideas 의 축약어로 해석할 사람은 없다.
   md 를 markdown 말고 monday 로 해석할 사람은 없다.
   이렇게 축약어가 오해를 일으키지 않으면 써도 된다.

> 방향성: 최소한의 내장 문법 + 최대한의 확장 자유. 사용자가 제어 구조까지
> 직접 만들 수 있는 언어.

인자 함수·국·소국(및 특국) 체계의 핵심 이점은 **코드를 유연하게 짤 수 있다**는 것이다.

## 코딩 처음 하는 사람

코딩 처음 하는 사람들이 보는 섹션이다. 이미 코딩을 할줄 알거나 숙력된 파워유저라면 본격적으로 배우기 섹션으로 넘어가길 권한다. 이미 코딩 배울때 다 알 문법이다.

블럭코딩 하는 사람이든 처음 코딩 하는 사람이든 똑같다. 그냥 이 섹션부터 봐라 블럭코딩이든 처음 코딩 하는 사람이든 언어코딩으로 가면 똑같이 헷갈려 한다. 블럭코딩은 코딩이 뭐인지만 알게 해주는거지 개념을 익히는게 아니다.

// TODO 만들기

## 본격적으로 배우기

Tennin 은 모든게 인자함수다. 진짜 모든게 인자함수 그냥 if 문 뒤에 블럭도 인자함수, smalltalk 로 생각하면 쉽다. smalltalk 는 블럭도 1급 시민이다. 인자함수도 비슷하다. smalltalk 에 블럭 그리고 거기에 인자를 넣을수 있는것이다. 그러니까 기존 언어들은 if 뒤에 {} 는 특수문법이고 제어문 if, while 이게 다 특수 문법이다. 그런데 그러면 문제가 있다. 제어 구조를 어떻게 만드냐, 진짜 이상한 goto 덩어리에 이상한 while 문 2겹 이게 좋나? 그냥 아주 싫을것이다. 그래서 Tennin 은 Lisp 같이 인자 함수 라는것으로 if, while 이런것들까지 다 함수로 해놨다.

비유하자면 이런거다. 우리가 맨날 다니는곳에 길이 있다. (if, while 이런 제어문 이런것들) 그런데 이러면 문제가 있다. 이 내가 가는데에 맞는 (이 프로젝트에 맞는) 길을 만들기가 어렵다. (이 프로젝트에 맞는 제어문을 만들기가 어렵다.) Tennin 은 그런거다. 길을 새로 만들수 있다. (이 프로젝트에 맞는 새로운 제어문을 만들수 있다.) 이러면 초기에 그 길을 만들땐 힘들겠지만 사용할땐 (프로젝트에 만든 그런 제어문 사용할땐) 오히려 편하다. 이게 Tennin 이다.

먼저 인자함수 보자. 인자함수는 4가지 형태가 있다. 1형, 2형, 2-2형, 3형 그리고 이 인자함수는 제어구조 블럭 구현때문에 있다.

먼저 인자함수 **1형 이다.** `"aa"; "bb"` — 값들. 값이 인자로 전달된다. (`("aa"; "bb"):{}` 의 축약)\
그다음 **2형 이다.** `("aa"; "bb")` — 괄호로 묶은 값들(`;` 또는 `,` 로 구분). 인자로 전달된다.\
그리고 그냥 블럭만 표현할때 쓰는 **2-2형 이다.** 인자 없이 내용만 적는 형태. `():{내용}` 에서 빈 인자 `()` 를 지운 축약이다. `else`, `expand` 처럼 받을 인자가 없는 국이 여기 해당한다.\
그리고 진짜 최종적으로 다 넣어야 할때 쓰는 **3형 이다.** `(인자들):{내용}` — `()`=인자들, `{}`=내용. 인자 함수의 기본 형태. 매개변수·반환타입 슬롯은 기본적으로 생략된다(매개변수는 소국으로 주입, 기본적으로 반환 개념 없음).

1형은 간단히 값을 적을때, 리터럴을 적을때 사용된다. 2형은 쫌 복잡하게 at 이 사용되고 뭐 값을 정확히 명확히 보여줘야 할때 사용한다. 2-2 형은 그냥 코드만 필요할때 사용하고 최종형 3형은 코드와 인자도 필요할때 사용한다.

그러니까 인자함수를 쓸때 직접 적어놔야 하는 정도에 따라서 저걸 쓰면 된다. 인자만 필요한데 인자도 간단한 값이면 1형, 인자함수인데 인자가 쫌 복잡해 보이면 2형 이렇게 쓰면 된다. 선택은 사용자의 몫이다.

**1형, 2형에 `,` 로 구분 vs ; 으로 구분**: 그냥 함수 호출할땐 , 으로 구분하고 뭐 복잡하게 인자함수의 인자 부분에 인자가 많이 들어가고 줄넘김이 많이 있을때는 ; 으로 구분을 추천한다. 강제는 아니다 만약에 \n 있을시 l으로 구분 그런거 있으면\
/\s/ 는 무시 원칙이 깨지기 때문이다.
