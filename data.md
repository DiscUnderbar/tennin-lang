# data.md — lmnop 설계하면서 발견한 것들

> 이 언어를 설계하는 과정에서 얻은 관찰·통찰을 모아두는 곳.
> 기획(LanguageDesignDocument.md)이 "무엇을 정했나"라면, 여기는 "왜 그렇게
> 생각하게 됐나 / 도중에 뭘 깨달았나"를 기록한다. 나중에 방향이 흔들릴 때
> 근거로 되돌아보기 위함.

---

## #1. "실사용 vs 습작"과 "닫힌 코어 vs 열린 코어"는 다른 축이다

**맥락**
lmnop 기획(함수 1급 시민, 제어구조도 콜백, 이름 매직 금지, 파일=무엇이든)과
습작으로 급하게 만든 인터프리터(붙박이 키워드 다수, 함수는 이류 시민, `if`/`while`
전용 문법)를 비교하다 나온 관찰.

**처음 생각**
둘의 차이는 그냥 "실제 사용을 목적으로 기획하냐 vs 그냥 만들어보냐"의 차이다.

**다듬은 결론 — 축이 하나가 아니라 둘이다**

1. **실사용 vs 습작** 축
   - 여기서 갈리는 것: 구현 방식(네이티브 컴파일 vs 인터프리터), 성능,
     표준 라이브러리 범위.
   - 습작은 "언어가 도는 원리를 빨리 보여주기"가 목적이라 만들기 쉬운 쪽으로
     흐른다.

2. **닫힌 코어 vs 열린 코어** 축
   - 여기서 갈리는 것: 함수가 1급 시민인가, 제어구조를 사용자가 함수로 정의할
     수 있는가, 이름 기반 매직을 허용하는가.
   - "닫힌 코어" = `if`/`while`이 못 바꾸는 붙박이 문법. "열린 코어" = 그런
     것조차 사용자가 다시 정의 가능.

**핵심 증거: 이 두 축은 독립이다**
"실사용"이 곧 "열린 코어"를 의미하지 않는다.
- Go, (초기) Java는 명백히 실사용 언어지만 `if`/`while`은 못 바꾸는 닫힌 코어다.
  오히려 팀 단위 협업에서는 "아무나 자기 제어구조를 만들 수 있는" 자유도가
  코드를 읽기 어렵게 만들 수 있어서, 일부러 닫아두는 것이 실사용에 유리하다고
  본 설계다.
- 반대로 Lisp/Scheme은 대표적인 "열린 코어"지만, 그 자유도와 고성능(네이티브
  수준)은 역사적으로 늘 긴장 관계였다.

**그래서 lmnop에 주는 의미 (장점 vs 단점, 두 마리 토끼)**
lmnop 기획은 두 축에서 각각 **"실사용" + "열린 코어"**를 동시에 골랐다.
- 장점: 자유도(사용자가 제어구조까지 확장) + 실전 성능을 둘 다 노린다. 성공하면
  희소한 조합.
- 단점/위험: 이 조합은 난이도가 가장 높다. 1-A(성능 최우선)와 1-B(최대 자유도)가
  정면으로 부딪힐 수 있다. 열린 코어는 보통 런타임 유연성(동적 디스패치, 클로저
  남발)을 요구하는데, 이는 성능 최우선과 상충한다.
- **결론: 이 언어의 핵심 승부처는 "열린 코어를 유지하면서도 컴파일 타임에
  최대한 확정지어(정적으로) 성능을 지키는 방법"을 찾는 것이다.** 예: 제어구조가
  콜백이어도, 컴파일러가 그 콜백을 인라인/특수화해서 붙박이 문법만큼 빠른
  기계어를 뽑아낼 수 있어야 한다.

**미해결 질문**
- 콜백 기반 제어구조를 어디까지 컴파일 타임에 인라인할 수 있는가?
- "열림"을 어느 선까지 허용할 때 성능이 무너지지 않는가? (전부 열면 Lisp의
  성능 문제를 답습, 너무 닫으면 기획 1-B를 배신.)

---

## #2. 더 일반적인 기능이 있으면 특수 기능은 "중복"이다 — float 제거 사례

**맥락**
타입 표를 점검하다가 세 가지가 동시에 걸렸다.
- `float`(단정밀도, 32비트)와 "일부 제외 모든 타입은 64비트"라는 서술이 충돌.
- 폭(width) 지정용 제네릭이 이미 있음: `byte[T]`=8, `short[T]`=16,
  `middle[T]`=32비트. (원래 `<>`였으나 비교 연산자와 충돌해 `[]`로 변경.)
- 그렇다면 `middle[double]` = 32비트 double = **단정밀도** = float 과 정확히 같다.

**깨달음**
`float`은 독립된 타입이 아니라, 이미 존재하는 더 일반적인 도구(`middle[T]`)의
한 특수 경우에 불과했다. 즉 `float`은 처음부터 **중복**이었다.

**그래서 내린 결정**
`float` 타입을 제거하고, 단정밀도가 필요하면 `middle[double]`로 표기한다.

**왜 좋은가 (두 마리 토끼)**
1. **충돌 해소.** "float=32비트 vs 모든 타입 64비트" 모순이 사라진다.
2. **중복 제거 (기획 1-A #10 준수).** 비슷한 기능을 따로 두지 않는다는 원칙을
   오히려 강화한다. 특수 타입 하나를 지운 대신, 일반 도구 하나로 표현한다.
   → 언어의 표면적(외워야 할 키워드/타입 수)이 줄어 가독성(#2)에도 이득.

**단점 / 트레이드오프**
- `float` 한 단어보다 `middle[double]`이 길다. 자주 쓰는 타입이면 타이핑·시각
  비용이 늘 수 있다. (단, lmnop은 "짧은 이름"보다 "중복 제로"를 우선하므로 감수.)

**일반화한 교훈 (다음에도 적용)**
> 새 타입/키워드/문법을 추가하려 할 때, "이미 있는 더 일반적인 도구의 특수
> 경우 아닌가?"를 먼저 묻는다. 맞다면 그건 새 기능이 아니라 중복이다.
> 특수 기능을 지우고 일반 도구로 표현하는 쪽이 거의 항상 이득이다
> (충돌 감소 + 표면적 감소). — float → middle[double] 이 첫 사례.

---

## #3. 모호함은 "표식 추가"가 아니라 "불가능한 조합 금지"로 없앤다

**맥락**
인자함수 축약형(1~7형)이 여럿이라 "이게 몇 형인지"가 모호할 줄 알았다.
처음엔 "괄호 개수에 따라 첫 소괄호의 역할(매개변수/내용)이 바뀌므로, 파서는
앞을 내다봐야 하고 사람은 괄호를 세어야 한다"고 걱정했다.

**깨달음 — 모호함을 없애는 길은 둘이다**
1. **표식(마커·구분자)을 추가한다.** 흔한 방법이지만 문법이 길고 지저분해진다.
2. **말이 안 되는 조합을 아예 금지한다.** 표식 없이도 남는 조합이 서로 안
   겹쳐 유일해진다.

lmnop은 2번을 택했다. "매개변수만 있고 내용 없음", "내용 없고 반환타입만 있음"을
금지하니, (`{}` 유무 × 소괄호 개수)만으로 형이 유일하게 결정된다. 예: `{x}:(a):(b)`
는 `{}`가 있고 소괄호 2개이므로 무조건 6형(내용, 반환타입) — `(a)`가 매개변수일
여지가 없다. 파서 lookahead도, 사람의 개수 세기도 필요 없다.

**왜 좋은가 (두 마리 토끼)**
- **간결함(마커 0)** 과 **명확함(모호성 0)** 을 동시에 잡는다.
- 이는 1-A #11("중복이 생길 여지 자체를 없애라")과 같은 사고의 확장이다.
  #11이 "기능 충돌의 여지를 없앤다"면, 여기선 "구문 모호성의 여지를 없앤다".

**단점 / 트레이드오프**
- 이 무모호성은 "형-집합을 한 번 외워야" 성립한다. 파서·숙련자에겐 확실하지만,
  규칙을 모르는 초심자에겐 여전히 낯설 수 있다. (무모호 ≠ 무학습비용.)

**일반화한 교훈 (다음에도 적용)**
> 새 축약·문법을 넣을 때 "표식을 더 붙일까"보다 "불가능한 조합을 금지해
> 유일하게 만들 수 있나"를 먼저 본다. 조합을 잘라 얻은 무모호성이 마커보다
> 대개 간결하다.

**닫는 노트 — #1 승부처가 여기서 닫힌다**
이 v0.2 전체(원칙 0의 단일 구문 통일 + 원칙 5의 핫패스 인라인)는 #1에서 열어둔
승부처 — "열린 코어를 유지하며 성능 지키기" — 에 대한 구체적 답이 됐다.
의미론은 인자함수로 통일(열림), 성능은 컴파일러 인라인으로 회수. #1의 미해결
질문이 여기서 닫힌다.

---

## #4. lmnop 함수는 왜 유연한가 — "이름 붙은 인자함수"라는 단일 모델

**맥락**
func 선언 문법을 정하기 전에, lmnop 함수가 무엇이 유연하고 무엇이 좋은지,
그 장단점을 먼저 정리한다. 이게 "func가 무엇을 받아야 하는가"의 판단 기준이 된다.

**핵심 통찰**
lmnop에서 "함수"는 별도 범주가 아니라 **이름(naming)이 붙은 인자함수**일 뿐이다.
유연성은 전부 이 단일 모델에서 파생된다. 즉 함수를 특별 취급하지 않는 것이
오히려 함수를 가장 강력하게 만든다.

**유연성의 원천 (각각 두 마리 토끼)**
1. **단일 구문** — 함수·제어구조·클래스·리터럴이 전부 같은 형식이다. 사용자가
   `if`/`while`/`switch`까지 함수로 정의할 수 있다.
   - 토끼: 통일된 의미론(배울 건 하나) + 최대 확장성(제어구조까지 재정의).
   - 대가: 슬롯·형(型) 규칙을 익혀야 한다.
2. **옵션 슬롯** — 기본형 `{인자}:(내용)`, 필요하면 매개변수·반환타입으로 확장.
   - 토끼: 흔한 콜백은 간결 + 복잡한 함수는 표현력 유지.
   - 대가: 슬롯 개수가 달라져 파싱/읽기 규칙이 필요(→ #3의 금지-조합으로 해결).
3. **naming** — 이름을 데이터로 분리. 인자로 받기 어려운 것(이름표)도 받는다.
   - 토끼: 이름/데이터 분리(가독성) + 이름을 데이터처럼 다룸(유연성).
4. **국 / 소국** — 다단계(`if`→`else`) + 문맥 종속(`break`/`continue`)을 문법화.
   - 토끼: 강력한 제어구조 표현 + 스코프 안전(문맥 밖 호출 원천 차단).
   - 대가: 국·level 개념 학습.
5. **return 스위치** — 반환타입을 켜야 `return`이 산다.
   - 토끼: 값 반환 함수와 순수 부작용 함수를 같은 문법으로 + 숨은 제어흐름 없음.
6. **comptime — 컴파일타임 실행** — 코드를 컴파일 시점에 실행(상수계산·컴파일타임 조작 등).
   - 토끼: 컴파일타임 표현력. (**관점 갱신**: 제네릭·매크로·상수계산은 서로 *다른* 기능이다 — 컴파일 시점만 공유할 뿐 하나로 '흡수'되지 않는다. §1 반영.)
7. **함수 1급 시민 + 파일도 함수** — 함수를 값으로 넘기고, 파일 자체가 함수/클래스가
   될 수 있다. 최대 자유도(1-B #6·#7).

**정직한 단점**
- 유연성의 대가는 학습 곡선이다(슬롯·국·level·naming·comptime을 다 익혀야).
- 너무 자유로우면 코드가 사람마다 제각각이 되어 팀 가독성이 떨어질 위험이 있다
  (열린 코어의 근본 긴장 — #1 참고).
- 콜백 남발 시 성능 우려 → 핫패스 인라인(원칙 5)으로 회수한다.

**결론 (다음 단계로 연결)**
함수가 "설정하는 것"은 결국 이 단일 모델의 구성요소로 환원된다:
**naming · `{}`(인자) · (매개변수) · (내용) · (반환타입)**.
받는 콜백은 "함수 타입 매개변수"로, `self`는 "첫 매개변수"로 흡수되므로 별도
항목이 아니다. 즉 func가 받아야 할 것은 새로 만드는 게 아니라, 이미 있는 슬롯
구조 그대로다.

---

## #5. lmnop의 목적과 해자 — "하나의 형태로 통일된 자유 × 정직한 네이티브 성능"

**목적**: 다른 언어의 시그니처 기능까지 lmnop의 체계적 함수(인자함수)로 **간단히
재구현**할 수 있어야 한다. lmnop은 사실상 **메타언어** — 남의 좋은 기능이 나오면
컴파일러를 기다리지 않고 인자함수로 바로 구현한다.

**경쟁자**: Rust, C++, Zig. 생태계(일자리·라이브러리·툴·커뮤니티)로는 신생
언어가 절대 못 이긴다. 넘어올 유일한 이유 = 아무도 안 가진 조합:
- **하나의 형태로 통일된 자유** — Lisp처럼 뭐든 정의하되(제어구조까지), 정의한
  모든 게 같은 인자함수 형태라 방언으로 안 쪼개진다. (원칙 0의 전략적 의미 =
  자유를 주면서 파편화를 막는 유일한 장치.)
- **정직한 네이티브 성능** — Zig처럼 비용 노출·숨은 흐름 없음 + 핫패스 인라인.

**규율 (universality by structure)**: 남의 기능을 지원하려고 인자함수에
**특례를 추가하지 않는다.** 기능은 이미 있는 구조에서 파생돼야 한다. 막히면 →
필요한 게 "여러 기능이 공유하는 범용 원시요소"면 추가 허용, "이 기능 전용
특례"면 금지.

**두 축 (범위 정직하게)**:
- **문법·제어·추상 축** — 인자함수가 거의 다 흡수(switch/match/defer/yield/제어구조).
- **타입·정적분석 축** — Rust 소유권 같은 전역 분석은 인자함수 밖. 타입시스템/
  comptime 영역이며, lmnop이 그런 안전성을 원하는지 자체가 별도 결정.

---

## #6. 표현력의 목표는 "최소 알파벳" — 가능성이 아니라 ergonomic-최소가 진짜 어려움

**비유**: 단어(apple, cat)를 다 외우는 대신, 음소 ~40개가 조합돼 무한한 단어를
만든다. lmnop도 "기능마다 규칙"이 아니라 "작고 고정된 원시요소 알파벳이 조합돼
무한한 기능".

**이론적 안심**: 하나의 규칙으로 전부 표현하는 건 **가능함이 증명됨** — 람다
계산법(추상+적용 두 규칙 = 튜링 완전). 제어 축의 만능 원시요소도 알려져 있음 —
delimited continuation / effect handler 하나로 제너레이터·async·예외·코루틴·
백트래킹이 전부 파생.

**그래서 진짜 어려움은 "가능한가"가 아니다**(그건 정복됨). **"흔한 걸 짧고·읽기
좋고·빠르게 나오게 하는 알파벳을 고르는 것"** — 정답 없는 설계·취향 문제.
최소만 좇으면 람다 계산법처럼 순수하지만 못 쓰고, 편의만 좇으면 기능 덩어리.
**최소이면서 쓸 만한** 좁은 능선을 걷는 것.

---

## #7. 컴파일 수준을 열어주면서 High-Level을 유지하는 법

**문제**: 만능·최소 원시요소는 보통 저수준(연속·람다). 근데 lmnop은 쓰기 쉽고
읽기 좋아야 한다. 다른 언어에서 메타프로그래밍이 흉한 이유 = 코드가 수십 종
AST 노드 동물원이고 그 *표현*을 만지기 때문.

**해법 (맞물린 조각들)**:
1. **원칙 0 → homoiconic 균일성.** 모든 코드가 인자함수 한 모양. "구조를
   프로그래밍"이 "인자함수 조합"이 되고, 평범한 lmnop 코드로 하나의 모양 위에서
   한다. AST API로 추락 안 함.
2. **핵심 꼼수: 받은 블록에 소국 주입 + 실행 제어.** for(break/continue),
   제너레이터(yield), 스크립트(..), 에러(throw). AST 안 만지고 "이 블록에 이
   소국 넣고 이렇게 돌려라"로 고수준 표현. 제어·문맥 기능 대부분을 덮는 한 패턴.
3. **층 나누기.** 저수준 원시요소는 바닥(feature-author만 만짐), 읽기 좋은
   기능은 그 위 인자함수(일상 사용). High-level in daily use, low-level-capable
   at foundation.
4. **저수준 추락 방지 규율.** (a) comptime = 런타임과 같은 문법. (b) 별도
   저수준 컴파일러 API 금지 — 기능 정의도 평범한 인자함수 연산으로. (c)
   원시요소는 "읽기 좋은 정규 표면과 함께일 때만" 채택.

**남은 난제**: "블록 받기·소국 주입·실행 제어" 인터페이스 자체가 균일하고 읽기
좋아야 성립한다. 여기가 진짜 설계 능선.

---

## #8. 경쟁·철학·고통을 가르는 세 갈래 (λ 람다 절벽 연구에서)

**맥락**
LanguageDesignTechnologyData.md(λ 람다 절벽 / 가시절벽 연구)를 lmnop에 비춰보다,
"경쟁자를 생각하는 게 가시절벽(오르기)의 함정 아니냐", "우리 '왜'가 철학에서
나오면 위험하지 않냐"는 두 우려가 나왔다. 둘 다 **틀린 우려**였고, 갈라야 한다.

**① "쓸만한가"(바깥·목적) ≠ "위대해지려"(안쪽·가시)**
- 가시절벽은 *"위대하다고 평가받으려고"* 만들 때 찔린다 — 자기참조적(Astra가
  "독창성 10으로 평가될 것"이라 쓴 손).
- 경쟁자·시장을 보는 건 방향이 반대다: *"진짜 프로그래머가 이걸 쓸까?"* — 바깥의
  필요를 향한다. 이건 조건 1(필요가 먼저)이고 언어의 존재 이유 그 자체다.
- **안 쓸 언어면 만들 이유가 없어지므로, "쓸만한가"가 절벽 통과보다 더 근본이다.**
  절벽은 *창작의 질*을 재고, 쓸만함은 *존재 목적*을 잰다 — 다른 축. (연구의
  구현가능성·실무 렌즈가 후자.)
- 경계선: 경쟁 명확성을 **설계 나침반**("이 결정이 정체성을 흐리나?")으로 쓰면
  정당. "X를 이길 언어가 필요해서"로 넘어가면 가시. → 나침반으로만 쓴다.

**② 고통 → 철학(정당) vs 철학 → 문법(함정)**
- 진짜 기준은 "철학이냐 고통이냐"가 아니라 **순서**다.
  - **고통 → 철학**: 겪은 걸 요약해 철학이 됨. 정당.
  - **철학 → 문법**: 철학을 먼저 세우고 문법을 끼워 맞춤. 함정(LMP `increase x by 1`).
- 연구 조건 4가 그대로다: *"철학은 이유들이 쌓인 뒤 사후 발견. Rust 소유권도
  필요가 먼저, 철학은 나중에 붙은 이야기."*
- 그러므로 **좋은 철학은 결국 고통·제약·도메인에 '대한' 철학**이다. lmnop의
  자유성도 "제약 많은 언어에서 유연하게 못 짜 반복적으로 막혔던" 고통에서 나온
  것이라 정당하다(고통→철학).

**③ 검증 기준 교정: "철학인가?"가 아니라 "고통으로 되짚어지나?"**
- 우리 "왜"를 감사할 때 물을 것: **이 철학이 구체적 고통("언어 X에서 Y를 못 해
  막힘")으로 되짚어지나?** 되짚어지면 통과, 되짚을 고통이 없으면 그때가
  *상상한 고통*이다. (검증은 남기되 과녁을 제대로 맞춤 — #5의 audit 아이디어 교정.)

**한 줄 결론**
경쟁은 "쓸만한가"를 묻는 바깥 검증이라 정당하고(가시 아님), 철학은 고통의
결정(結晶)일 때만 정당하며, 그 판정은 "고통으로 되짚어지나"로 한다.

## 큐와 스택의 구조 유래

내가 Claude 에게 어덯게 가야지 점점더 잘 하는 제대로된 개발자 길로 갈수 있을까 물어봤다, 그때 그 Claude 가
제안했던게 간단한 SNS 앱 만들기, 그리고 그렇게 나는 Room 이라는 SNS 앱을 설계했는데 솔찍히 그때 쫌
설계를 과하게 처음 하는 사람 치고 과하게 했다 zevra 라는 언어로 이 SNS 앱에 확장팩을 만들어서 커스터마이징이
가능하게 리눅스처럼 그런데 이때 이 큐와 스택 지금 1-D 에 있는걸 생각했다 그런데 이게 독이 되지 않았다 오히려
너무 방식이 좋은거야 그래서 한번 심심해서 Claude 에게 말해봤다 이거 zevring 이라는 브라우저의 실행 동작으로어
떠냐고 (또 zevring 이라는 이름은 Room 터미널에서 zevra 코드를 돌릴떄 사용하던 키워드였다)
그리고 그렇게 좀더 구체적으로 이 실행 방식을 짜게되고 또 그런데 이게 진짜 lmnop 기획할떄도 너무 좋았다.
그래서 그냥 이 방식으로 계속 가자고 한거다

## 출력 zevra 의 호완성

출력은 print 함수나 Line.addLine 함수를 사용하면 되고 이 두개의 출력 함수는 zevra 에서 왔다. (zevring 에서 사용하는 zevra 와 호완성을 위해)\
`Line.addLine("Hello, World");` 나 `print("Hello, World");` 둘다 가능하다. 참고로 이건 착오로 생긴 두개의 함수인데\
zevra 에서 출력하는 함수를 만들때 두 사람중 한명은 print 함수를 만들었고 다른 한명은 원래 이 ts 에서 사용하던 Line 모듈과 거기에 출력 함수 addLine 을 zevra 에 가져가\
Line.addLine 을 만들었는데 그렇게 출력 키워드는 zevra 에서 두개가 되었고 zevring 에서 메인으로 사용하는 zevra 코드와 호완성을 위해 lmnop 에서도 이 두 키워드를 이어받았다.\
참고로 그냥 이게 제대로 작동되나 보는 출력은 print 를 사용하고 그냥 진짜 쓸떄는 Line.addLine 을 쓰는걸 권장한다.

## #9. 왜 Tennin 은 Lisp 식 방언 파편화를 덜 겪나 — "고정 문법 + 확장 어휘"

**사실 정정 먼저:** Tennin 도 사용자가 제어구조를 직접 정의할 수 있다 (원칙 0, 1-B #2·#5). Lisp 을 파편화시킨 그 자유가 Tennin 에도 있다. 그러니 "누구나 못 만든다" 는 틀리다 — 만들 수 있다. 진짜 물음은 "그 자유가 왜 Lisp 처럼 안 쪼개지나" 다.

**약한 근거 (재검토 후보):** "문법을 멋있게 만들어 표준을 쓰게 한다" 는 취향·철학이라 하중을 못 진다 (#8 규율: 철학 하나로 선 결정은 재검토 후보). 멋있음은 사람마다 달라서 방언을 막는 힘이 될 수 없다.

**하중을 지는 구조적 근거:**

- **Lisp 은 문법(표면)을 바꿀 수 있어서 쪼개졌다.** reader 매크로가 토큰화를 바꾸고 매크로가 임의의 구문 모양을 지어낸다 → 두 Lisp 코드가 다른 언어처럼 보인다.
- **Tennin 은 하나의 고정 문법 안에서 어휘만 늘린다.** 모든 것이 `이름 {인자}:(...)` 한 모양(원칙 0). 커스텀 제어구조도 그 모양이다. 사용자는 함수 이름을 등록할 뿐 렉서·표면을 못 바꾼다 → 아무리 커스텀해도 두 Tennin 코드는 **같은 문법**으로 읽힌다. 새 단어, 같은 문장 구조.
- 커스텀 구조는 모듈 스코프(전역 표면 변경 아님)라 폭발 반경이 작고, Feature Absorption 도 임의 매크로 표면이 아니라 그 한 모양으로의 **파생**이라 흡수된 기능도 균일하게 보인다.
- 즉 **두 마리 토끼**: 제어구조를 정의할 자유 × 표면 파편화 없음 — 자유를 한 형태로 흘려보내기 때문. (Lisp = 확장 가능한 문법 → 쪼개짐. Tennin = 고정 문법 + 확장 어휘 → 표면은 안 쪼개짐.)

**정직한 잔여 (우회 안 함):** 문법 균일성은 **의미·관용·생태계** 파편화까지는 못 막는다. 팀마다 다른 모듈 어휘(=사실상 라이브러리 방언)를 쌓을 수 있다 (Scheme↔CL 분열도 매크로만이 아니라 커뮤니티·생태계였다). 어떤 언어도 이건 완전히 못 막는다 — 강한 표준·관용이 완화하되 제거하진 못한다. 그래서 "멋있는 문법" 은 이 층에서 **약한 당김**으로만 쓸모 있고, 하중은 고정문법 구조가 진다.

**실전 완화책 (문법 취향 말고 구조로):**

1. 흔한 제어구조를 다 덮는 축복받은 표준 prelude — 애초에 직접 만들 필요를 없앤다 ("한 가지 분명한 방법").
2. 균일 표면 → 균일 툴링: 어떤 이름이 뭘 하는지 도구가 똑같이 보여줘 재발명 유혹을 낮춘다.
3. 남의 방언을 쓰려면 명시적 `take`(모듈 임포트)가 코드에 보이므로, 그 마찰이 표준을 기본값으로 유지한다.

## #10. 역발상 — 방언을 억제가 아니라 전략으로 (프로젝트별 언어)

#9 는 "왜 안 쪼개지나" 였다. 여기서 한 발 더: **프로젝트마다 자기 방언이 생기는 것 자체를 전략으로 쓴다.** 프로젝트마다 자기만의 함수·자기만의 FuncKind 를 두면 코드가 그 도메인의 언어로 말한다 — 그게 유지보수에 더 편하다.

**왜 (도메인 + 고통, 철학 아님):** 범용 언어로 도메인 코드를 짜면 도메인 개념이 매번 우회로로 표현돼 반복·오독이 생긴다 (M.G.S 오브젝트 스크립트, zevra 확장팩, ErrScript 아이템 DSL 에서 겪은 그 고통 — #8 "필요가 먼저"). 도메인에 맞춘 어휘(함수)와 범주(FuncKind — 도메인 규칙을 한 번 인코딩하고 도메인 소국을 자동 부착: 예 `FuncKind.add(COUNTRY_FUNC)` 로 세금·반란 함수 자동)로 두면 흔한 경우가 짧아지고 불변식이 강제된다. 이건 정확히 #5/#8 의 "네이티브 DSL 심기" 정체성의 실현이다.

**두 마리 토끼가 되는 이유 (#9 와 맞물림):** 보통 방언 = Lisp 의 저주(남이 못 읽음). Tennin 에선 방언이 **고정 문법 안의 새 어휘**라, 남은 문장 구조를 그대로 읽고 새 단어만 배운다. 게다가 각 단어는 등록된 함수라 점프해 읽을 수 있다(균일 툴링). 즉 Tennin 프로젝트 방언을 배우는 건 **새 언어 문법이 아니라 라이브러리 API 를 배우는 것**에 가깝다. 그래서 도메인-적합의 이득을 얻으면서 가독성 저주는 안 문다.

**정직한 포기 (우회 안 함):** 그래도 공짜는 아니다. 신규자는 이 프로젝트의 어휘를 배워야 하고, 그 어휘는 프로젝트를 넘어 이식되지 않는다 (범용 `if` 는 이식되지만 `FuncKind.COUNTRY_FUNC` 는 그 게임에만 산다). **버는 것**: 도메인-적합 유지보수 + 불변식 강제. **내는 것**: 신규자 온보딩 + 교차-프로젝트 어휘 비이식성. 승자는 문맥별로 갈린다 — 오래 사는 사내 프로젝트는 이득, 낯선 사람에게 여는 오픈소스·고회전 팀은 비용이 더 크다.

**온보딩 비용 완화 — Deep Module:** 위 "신규자 온보딩" 비용은 방언 어휘를 **Deep Module** 로 지으면 크게 준다. Interception·타입매직으로 내부 구조를 감추면 신입은 표면만 액면가로 쓴다 — 예: `if {i > 3}:()` 는 사실 `condition` 을 타입매직으로 받는 것이지만, 쓸 때는 그게 안 보인다. 즉 각 도메인 단어를 *어떻게 만들었나* 가 아니라 *무엇을 하나* 만 알면 된다 (좋은 API 는 속을 숨긴다).

- **정직한 잔여:** Deep Module 은 **항목당 깊이**(내부)를 없앨 뿐 **폭**(도메인 개념 수)은 못 없앤다 — 단어가 200개면 200개의 뜻은 여전히 배워야 한다(내부는 아니어도). 그리고 추상이 **샐 때**(에러 메시지에 `condition` 이 뜨거나, 성능 절벽, `.equations` 가 필요할 때) 숨긴 구조가 드러나 그때 배우게 된다 — 그 순간의 정보원은 **스펙**이다(타입매직 규칙: "호출부는 깨끗, 스펙이 정보원"). 이는 숨은 *구현*이지 숨은 *제어흐름*이 아니라서 원칙 3·비용 정직과 어긋나지 않는다.

**#9 와의 경계 규칙 (둘을 화해):** 표준 prelude 는 범용(재철자하면 낭비), 프로젝트 방언은 도메인(표현하면 정당). 판별 기준 = *그 어휘가 도메인 의미를 인코딩하나?* 그렇다 → 방언(전략). 범용 제어구조를 다시 철자할 뿐 → 낭비(prelude 가 덮음). 한 줄로: **"방언은 도메인을 말할 때 정당, 표준을 재철자할 때 낭비."**

## #11. FuncKind = 자연어의 품사

Tennin 의 FuncKind(함수의 종류 — LOOP/RETURN/DOING/사용자 정의 …)는 자연어의 **품사**와 같다. 명사·동사·형용사가 문장에서 하는 역할을 정하듯, FuncKind 는 함수가 코드에서 하는 역할(반복하는 것 / 값을 내는 것 / 무언가 하는 것 / 도메인 전용 …)을 정한다.

- **품사가 소수 고정이 아니라 언어마다 늘 수 있듯**, FuncKind 도 `FuncKind.add` 로 도메인 품사를 더한다 (#10 방언 전략과 정합 — 도메인 어휘 = 도메인 품사).
- **break/continue 같은 소국이 "그 품사(FuncKind)에서만 뜻이 있는" 것**은 품사-종속 활용과 닮았다 — 동사에만 붙는 어미처럼, LOOP 에만 붙는 break.
- 쓸모: "이 함수는 무슨 품사인가"가 곧 "이 자리에서 어떻게 활용되는가"라, 문법·소국·탈출(label) 규칙이 전부 FuncKind 를 읽는 게 자연어 문장 구조를 닮는다.
- **실질적 귀결 — 같은 품사는 같은 형식.** 영어에서 명사에 무언가를 붙일 때 `to` 라는 같은 형식을 쓰듯, **같은 FuncKind 의 함수들은 같은 슬롯 모양을 유지한다** (spec kind 규칙에 반영). 이게 품사 비유를 장식이 아니라 **강제되는 설계 규칙**으로 만든다: 하나를 배우면 그 품사 전체가 읽히고, 그래서 사용자가 만든 제어구조(원칙 0)나 프로젝트 방언(#10)도 남이 읽을 수 있다 — 새 언어가 아니라 새 어휘가 되는 지점.

-- *lmp*
-- *tenn*
