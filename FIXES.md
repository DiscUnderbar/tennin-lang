# FIXES.md — 남은 수정만 (문서 순서대로)

> 새로 저장된 1242줄 버전 기준. 이미 반영된 것(smalltalk 삭제, @=, 핫패스 교체, 섹션 신설)은 뺐다.
> 아래 문구는 붙여넣기 가능한 초안이다 — **최종 문장은 lmp가 다듬어 확정한다.** (CONTRIBUTING 결정 규칙)
> 적용한 항목은 CHANGELOG.md에 남긴다.

---

## 1. [0장 대상 사용자] 목적 문장 추가

**현재:** 없음 (신설)

**추가:**
> lmnop은 범용 언어다. 다만 첫 목적 프로그램은 게임이다 — 내가 원하는 방식으로 코드를 짜기 위해. 설계가 갈릴 때 "게임을 짤 때 어느 쪽이 필요한가"가 심판이다. C가 범용이면서 Unix를 심판으로 삼았던 것과 같다.
> lmnop은 zevring에서도 쓸 수 있지만 zevring 전용이 아니다 — 언어의 운명을 브라우저의 성공에 걸지 않는다. (zevring 전용 언어는 zevra다.)

---

## 2. [41행] 확정된 결정 — 컴파일타임 문구 교체

**현재:** "…하나의 컴파일타임 실행 모델(comptime 식)로 흡수한다. 별도 매크로·템플릿은 도입하지 않는다. …"

**교체:**
> **컴파일타임 메커니즘:** 제네릭·인자함수 커스터마이징·상수계산·인라인이 하던 일은 expand 하나가 한다. 별도 매크로·템플릿은 없다 — C식 텍스트 매크로가 아니라 의미 단위로 동작하는 컴파일타임 전개다. (1-A #10, 중복 금지)

---

## 3. [1-A 정보 지향 문단 끝] 두 층 명시 (한 문단 추가)

**추가:**
> 정보 지향은 두 층이다. 이름·위치·kind처럼 정적인 정보는 컴파일 타임 상수로 굽는다 — 비용 0. 정보 지향 큐·valbox는 진짜 동적인 질의에만 쓴다. condition·naming 캡처(타입 매직)는 정보 지향의 문법적 실현이다.

---

## 4. [1-A] 13 건너뜀 주석 + [1-B #8] 판정 기준

**추가 (1-A 번호 12와 14 사이):**
> @... 13은 불길해서 건너뜀

**추가 (1-B #8 끝):**
> 판정 기준은 "오해를 일으키는가"다. i·var·str처럼 널리 쓰여 오해가 없는 축약은 허용한다. monday→md처럼 다른 것(markdown)으로 읽히는 축약은 금지한다.

---

## 5. [원칙 0] 리터럴 문구 + mec→expand 이름 교체 (247행)

**추가 (원칙 0의 리터럴 설명 자리):**
> 리터럴은 내용 슬롯이 없는 특수 인자 함수다. 박아둘 코드가 없으므로 어디로든 갈 수 있어 1급이다. 1급/2류의 경계선 = 내용 슬롯을 갖는가.

**교체 (247–249행):** "성능 (mec)" → "성능 (expand)", 본문의 mec 세 곳 → expand.

---

## 6. [타입 섹션, condition 표 행 근처] 타입 매직 섹션 신설

**현재:** 타입 표에 `condition | 조건` 한 줄뿐.

**추가:**
> ### 타입 매직
>
> lmnop에서 이름은 기능을 담당하지 않는다 (이름 매직 금지, 1-B #3). 대신 타입이 기능을 담당한다 — 이것이 타입 매직이다.
>
> **인자의 평가 방식은 스펙에 선언된 타입이 정한다.**
>
> | 선언된 타입 | 받는 것 |
> |---|---|
> | (기본) | 평가된 값 — `i < 3` → `true` |
> | condition | 식의 구조 — `i < 3` → first = i, operator = Operator.lessThan, second = 3 |
> | naming | 이름 그 자체 — `VAR` → VAR라는 이름. 같은 이름의 변수가 있어도 이름이 이긴다 |
>
> condition 세부:
> - 필드는 값이 아니라 참조다 — 재평가가 가능하다. (`ref(< condition.first = i; condition.second = 3; >);` 모델)
> - 복잡한 식은 condition.equations로 부분식 리스트를 얻는다 (`(...condition)` 타입).
> - 단락: equations는 왼쪽부터 평가한다. and는 첫 거짓에서, or는 첫 참에서 멈춘다. (`x <> 0 and 10 ~/ x > 1`의 0나눗셈 방지가 여기에 걸린다.)
> - 성능: expand 문맥에서 구조는 컴파일 타임에만 존재하고, 런타임엔 기계어만 남는다. 구조를 런타임에 해석하면 Smalltalk식 감속이다 — 이 문장이 성능 방어벽이다.
> - 탈출: condition은 자기 국의 변수를 품는다 — 클로저와 같은 취급. 국 밖으로 나가는 경로는 send뿐이고, send하면 캡처된 변수가 함께 이주한다. 이주는 그 시점의 스냅샷이다 — 이후 원본이 바뀌어도 반영되지 않는다.
> - 정직한 단점: 호출부(`while {i < 3}`)만 봐서는 캡처 여부가 보이지 않는다 — 스펙이 정보원이다. 대신 호출부가 항상 깨끗하다. (`while condition (i < 3)` 같은 명시 표기는 더 복잡해져서 기각.)
>
> 왜 이름 매직은 금지고 타입 매직은 원리인가: 이름은 겉모습이고, 타입은 선언된 구조다. "기능은 명시적 구조로만 결정된다"(1-B #3)를 타입 매직이 그대로 지킨다.
>
> (선례: C#은 람다를 Expression 타입에 대입하면 코드 대신 식 트리가 잡힌다 — LINQ가 그 위에 서 있다. naming 캡처는 C#의 nameof, Lisp의 quote 계보. 타입 선언만으로 정해지는 것이 lmnop식이다.)

---

## 7. [연산자 섹션] 우선순위 표 추가 (확정본)

**추가:**
> ### 연산자 우선순위
>
> 높은 것부터:
>
> 1. `( )` 괄호
> 2. `^` — **우결합** (수학 관례: `2 ^ 3 ^ 2` = `2 ^ (3 ^ 2)`. 그리고 `-2 ^ 2` = `-(2 ^ 2)` = -4)
> 3. 단항: `bitnot`, 부호 `-`
> 4. 곱셈군: `*` `/` `~/` `%` `lshift` `rshift` `bitand`
> 5. 덧셈군: `+` `-` `bitor` `bitxor`
> 6. 비교: `==` `~=` `<>` `<~>` `>` `<` `>=` `<=`
> 7. `not`
> 8. `and`
> 9. `or`
>
> `^`만 우결합, 나머지는 전부 좌결합. 비교 연쇄(`a < b < c`)는 컴파일 에러다 (boolean과 int의 비교가 되므로 정적 타입이 자연히 잡는다 — 명시만 해둔다).
>
> 왜:
> - and가 or보다 위다 — 불 대수 관례(and = 곱, or = 합). 같은 순위면 `a or b and c`가 lmnop에서만 다르게 읽혀 모두가 놀란다.
> - 비트 연산이 수식군에 있다 — C가 40년 후회한 배치(`x bitand mask == 5` 함정)를 Go가 고친 방식이다. lmnop은 정적 타입이라 침묵 버그 대신 에러가 나긴 하지만, 괄호를 강제당하는 불편은 남으므로 수식군에 둔다.
> - bitnot은 단항이라 위에 있고, not은 Python처럼 비교 아래다 — `not a == b`가 `not (a == b)`로 읽히는 것이 자연스럽다.
> - and/or가 최하위라서 condition.equations의 최상위 분해층이 된다 — 타입 매직과 정합.

---

## 8. [790–792행] 출력 섹션 완성 ("또한"에서 끊긴 것)

**현재:** "출력은 `Line.addLine("...");` 을 사용한다. 또한" (미완)

**교체:**
> ### 출력
>
> 출력은 두 함수다: `Line.addLine`(범용)과 `print`(숫자 전용).
>
> print는 이름 그대로 pr + int — 숫자만 출력한다. 말장난에서 시작했지만 남긴 이유는 진지하다: 두 출력 함수의 겹침을 기능 분화로 없앴고(1-A #11 — 중복이 생긴 여지를 없앤다), zevra의 이름을 남겨 설계자 시그니처를 유지한다.
>
> 정직한 기록: 숫자 출력에는 여전히 두 길이 있다 — #10의 기록된 예외다 (이유: zevra 호환 + 개성).
>
> zevra 호환은 부분이다: zevra의 `print("문자")`는 lmnop에서 에러다.
>
> TODO: print의 숫자 범위(int만? double 포함?), 개행 여부.
> (두 함수가 생긴 착오의 역사는 data.md로.)

---

## 9. [job 섹션 끝] 경계 문장 추가

**추가:**
> job과 인자함수의 경계: **job은 국·스코프·수명을 만들지 않는다. 만들면 그건 인자함수다.** job은 단순 이름표가 아니라 의미를 바꿀 수 있는 표식이다 (파이썬 데코레이터처럼 — ref는 그 구간에서 `=`의 동작을 바꾼다).

---

## 10. [962–964행] 메모리 관리 섹션 완성

**현재:** SM 한 줄 소개뿐.

**교체 (전체):**
> ### 메모리 관리
>
> **end with free** — 국이 끝나면 그 국의 값은 즉시 소멸한다 (국 = 아레나). 이것은 문법이다: 소멸 시점이 코드에 보인다. GC처럼 실행을 멈추고 수색하지 않는다 — 해제 시점이 컴파일 타임에 확정된다.
>
> **send = 자동 이주** — 국의 값이 국보다 오래 사는 합법 경로는 send(이주)와 명시적 복사뿐이다.
> - send에 별도 표시는 없다. send 자체가 이주 명령이다.
> - 보낸 뒤 원래 자리에서 재사용하면 컴파일 에러다: "x는 N행에서 send로 이주했습니다."
> - 기본은 이동(포인터만 넘어감 — 비용 0). 복사는 명시한다 (문법 TODO).
> - 캡처를 품은 값(condition, 인자함수)은 send 시 캡처된 변수가 함께 이주한다 — 그 시점의 스냅샷이다.
>
> **탈출은 컴파일 에러** — send·복사 없이 국의 값을 밖으로 내보내는 모든 길은 막힌다: 반환으로, 컨테이너에 저장으로, 클로저 캡처로, condition 캡처로. 각 판정 규칙은 그 자리만 보고 결정된다(지역적·구문적). 전체 프로그램 분석이 필요해지면 그 규칙은 재설계한다 — 이것이 "러스트보다 쉽지만 안전"의 실측 기준이다.
>
> **SM (Substitution Memories)** — 저수준 코드는 SM이라는 가짜 메모리 위에서 짠다.
> - 대체 원칙: lmnop은 위험한 것을 금지하지 않는다 — 대체한다. VMware가 가짜 CPU를 주듯, SM이 가짜 메모리를 준다. 사용자에게는 대체물만 제공되고, 실제 메모리는 언어만 만진다.
> - `SM.store(주소, 값)` — 넣기 / `SM.load(주소)` — 얻기. (LLVM·하드웨어와 같은 용어. 인자 순서는 TODO.)
> - 범위 검사 2단: 컴파일 타임에 판정 가능하면(리터럴 등) 컴파일 에러. 나머지는 가드 페이지 — 범위 밖 접근은 하드웨어가 트랩으로 잡는다(검사 코드 0개, WASM 런타임 방식). 임베디드·32비트 폴백은 if 검사 — TODO.
> - 포기(기록): 커널·드라이버급 실메모리 직접 제어는 lmnop의 도메인이 아니다. 그 대가로 unsafe 없는 언어를 얻는다. C FFI는 SM↔실메모리 복사 계층(마셜링) — TODO.
>
> TODO: 복사 명시 문법, 참조/빌림 규칙(func·큐 문법과 함께), 탈출 각각의 최소 코드 + 에러 문안.

---

## 11. [968–970행] ## mec → ## expand 개명 + 내용 완성

**현재:** 세 줄 ("comptime, inline, 추론 그런것들을 다 매크로라고 한다…")

**교체 (전체):**
> ## expand
>
> expand는 특수 함수다 (var·const처럼 사용자가 만들 수 없는 함수).
>
> expand X는 "X를 컴파일 타임에 아는 만큼 실행하고, 모르는 만큼 그 자리에 전개하라"다.
> - 전부 알면 → 값만 남는다 (comptime/상수계산)
> - 아무것도 모르면 → 본문이 통째로 그 자리에 붙는다 (인라인)
> - 표기를 조작하는 함수면 → 치환 (at이 이 종류다 — `… = (…)`을 넣어주는 문법 설탕)
>
> 하나의 의미론에 결과가 셋이다 — 1-A #10(중복 금지)의 실현이다. (이런 방식을 부분 평가라고 부른다. Zig의 comptime이 같은 계보다.)
>
> 전개는 실패하지 않는다 — 모르는 것은 에러가 아니라 남는 코드다. 유일한 에러는 전개 한도 초과다. 깊이를 모르는 재귀는 복붙이 끝나지 않기 때문이다 (어떤 컴파일 타임 시스템도 못 피한다 — Zig도 quota로 막는다). 한도는 expand가 인자로 받고 기본값은 2000이다.
>
> 다메커니즘 함수(if, while …)는 expand로 정의된다. 그래서 제어구조의 호출 비용이 0이다.
>
> 대외 설명 첫 줄: expand는 텍스트가 아니라 의미 단위로 동작한다. C식 텍스트 매크로가 아니다.
>
> TODO: 전개 한도의 단위(전개 1회 = 무엇을 세나), 정의-expand와 호출-expand의 표기 — func 문법 확정 때.

---

## 12. [kind 섹션] 0/1 모순 수정 + 규칙 4개 추가

**교체 (984행):** "원래 kind.A 는 1 이다, kind.B 는 2 다" → "원래 kind.A는 0이다, kind.B는 1이다"
**교체 (992행):** "이때 Keyword.VAR 는 1 이라는 값이 저장된다" → "이때 Keyword.VAR는 0이라는 값이 저장된다" (993행 "데이터는 0", 1032행 "마지막 인덱스 3 → 자동 4"와 이제 정합)
**교체 (1038행):** "kind 함수는 inline 으로" → "kind 함수는 expand로"

**추가 (kind 섹션 끝):**
> - kind 비교는 `==`를 쓴다. (`~=` 예시는 이해용이다. 참고: 서로 다른 kind라도 index가 같으면 `~=`는 true다 — `~=`의 일관된 귀결이니 함정 주의.)
> - add는 expand 시점(컴파일 타임)에 실행된다. 사용자 소스에서의 추가는 자유다 (예: 나라 게임의 `FuncKind.add(COUNTRY_FUNC)` — 세금 계산, 반란 확률 같은 나라 함수 분류). 프로그램 실행 중 추가는 불가 — break/continue의 level 규칙이 kind를 읽기 때문이다.
> - 같은 이름을 add하면 컴파일 에러다 (1-A #11).
> - 새 kind에는 기본 규칙이 붙지 않는다 — 그 kind를 읽는 쪽이 의미를 준다.

**확인:** FuncKind의 `INLINE` 멤버 이름도 `EXPAND`로 바꿀지 (expand 결정과 통일).

---

## 13. 잔글자

- [1219행] UNKOWN.md → UNKNOWN.md
- [3-A] CONTRIBUTING 인용을 v2.1 파일과 동기화 (파일이 원본, 문서엔 요약+참조만도 가능)
- 오탈자 전수: FucKind → FuncKind, countrys → countries 등
- 모든 적용 건 CHANGELOG.md에 (날짜 / 전 → 후 / 왜 / 버린 것)

---

## 14. UNKNOWN.md에 넣을 항목 (복사용 전문)

> ## 등록부와 end with free의 공존
>
> Interception으로 클래스 인스턴스 등록부를 만들 수 있다 (모든 Brick을 아는 자료구조 — M.G.S where용). 그런데:
> - 등록부가 객체를 소유하면 → 객체가 자기 국이 끝나도 안 죽는다. 게임이 오래 돌수록 메모리가 영원히 쌓인다.
> - 등록부가 소유 없이 가리키기만 하면 → 객체가 국과 함께 죽는 순간 등록부는 죽은 메모리를 가리킨다 (dangling). 우리가 막으려는 바로 그 위험이다.
>
> 다른 언어들은 이걸 약한 참조로 푼다 — 소유하지 않아서 객체를 살려두지 않고, 객체가 죽으면 스스로 무효(null)가 되는 참조. 또는 소멸 훅(객체가 죽을 때 등록부에서 자동 제거)으로 푼다.
>
> lmnop 방식의 답을 아직 못 정했다. 국 소멸(end with free) 모델에 약한 참조나 소멸 훅을 어떻게 넣을지 모르겠다.

---

## 15. 검수 추가분 (빠짐 확인에서 발견)

- **[255행] 1형 예시 쉼표 → `;`.** 현재 `"아아", "비비"` — "인자는 `;`" 규칙과 어긋남. → `"아아"; "비비"`
- **[474행] `^` 제약 완화.** 현재 "x, y는 모두 int(정수)여야 한다" — 실수로 확인됨. → "x, y는 숫자(int·double)면 된다." (TODO: double 지수의 결과 타입)
- **[국/소국 섹션] 같은 번호 국의 반복·릴레이 규칙 추가 (확정분인데 이 파일에서 빠져 있었음).**
  > 같은 번호의 국이 여러 개면(예: else if 반복) 적은 순서대로 실행된다. 국간 통신도 그 순서를 따라 릴레이된다: 1국 → 첫 2국 → 둘째 2국 → …. else if 체인의 "앞에서 이미 실행됐으면 건너뛰기"가 이 릴레이(taken 전달)로 정의된다.
- **[201·712·730행] 클래스 moveTo 예제 — 보류 표시만.** 새 func 형식으로 갱신해야 하지만 func 표현 방식이 미정이므로, 지금은 예제 옆에 "@... TODO: func 문법 확정 후 갱신" 한 줄만 달아둔다 (까먹음 방지).
- **[620·629·759행] 정적 변수 PascalCase ↔ 클래스 PascalCase 충돌 — 재검토 후보.** 확정 수정이 아니라 결정 대기라 FIXES 성격이 아님. REVIEW.md의 이유 태그 감사(철학 태그 1순위)에 올라 있음.

참고: IDEAS.md · TODO.md · data.md로 옮길 목록은 REVIEW.md의 E 섹션에 그대로 살아 있다 (FIXES는 기획서 본문 수정만 담는다).