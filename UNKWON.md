미정 목록. UNKNOWN.md(방법을 모름): ① 등록부와 end with free의 공존 — 약한 참조/소멸 훅 (전문은 FIXES.md 14번에 복사용으로 있음) ② 다메커니즘 함수의 타입 표기법 ③ 참조/빌림 규칙(자식 큐에 빌려주기가 성립하는 조건) ④ Deep Module 로 감춘 내부(타입매직 condition·would, FuncKind, Interception)가 컴파일 에러 메시지로 튀어나와 방언을 쓰는 신입이 못 읽는 문제 — 에러를 표면 용어로 유지하거나 방언 저자가 에러 문안을 씌우는 법 (방법 미상) ⑤ Yulist `.키` 점 접근에서 키가 숫자일 때(`Yulist.0`) `.` 접근을 어떻게 허용·구분할지 ⑥ 국(나라) 안 슬롯 정의를 쉽게 해주는 문법 설탕 — 슬롯을 naming 으로 받아 컴파일하는 구조를 사용자가 매번 다 짜지 않게(원래 다 컴파일 구조를 작성), 단 ROP(표현 지향)는 유지하며 간단화 ⑦ Interception 각 상황(get/set/add/create…)이 넘기는 인자 shape 의 계약 — 어느 연산이 무슨 상황을 무슨 인자로 emit 하는지 정의(안 정하면 훅 인자 어긋날 때 에러 모호, Deep Module 문제와 연결); set 이 항상 {key;value} 인지 스칼라용 {value} 도 둘지; 여러 interception 겹쳐 낄 때 순서; 연산이 emit 하는 것 외에 **사용자가 직접 emit** 하는 것도 허용할지·문법; **훅의 return 이 원래 연산 결과로 흘러가는 방식**(더 고민). TODO.md(미룬 결정): func 표현 방식(국 필드 표기·확장 슬롯 파싱·정의/호출-expand 표기), 본문에서 국·condition 접근 표기, expand 한도의 단위, SM.store 인자 순서, 가드 페이지 임베디드 폴백, send 복사 문법, C FFI 마셜링, print 숫자 범위·개행, Yulist ? 표기와 프리셋 명세, FuncKind.INLINE→EXPAND 개명, double 지수 결과 타입, 탈출 4+1종의 최소 코드+에러 문안, 타입 이름 컨벤션 재검토(정적 변수 PascalCase↔클래스 충돌 포함), 그리고 1-D의 기존 미정들(큐 소유권 트리·failed 감독·data 큐 표기·atomic/tick).

-- *tenn*
