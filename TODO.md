확장슬롯을 만들자 일반적으로는 `{}:()` 가 최선의 슬롯들이다 그런데 확장 슬롯을 함수에서 사용하면 `{인자}:(매개변수):(내용)` 이런식으로도 받을수 있고 내용 대신에 **init** 이라고도 할수가 있다.\
아마도 나중에 함수 선언하는데 만들때 `{인자}:(매개변수):(init)` 이런식으로 그대로 받으면 될것 같다. 다른건 이제 이걸 뭐 `Slots.Arguments` 이런식으로 확장 슬롯의 이름을 지정하면 받을수도 있게 하면 좋을것 같다. (인자 함수 형식으로)

----------------------------------------------------------------------------

메크로를 추가하자 그러니까 대충 함수 느낌인데 컴파일 단계에서 이게 그냥 그 함수에서 내뱉는 코드로 컴파일 되는 그런식으로 아직은 그냥 대충 개념만 생각해봄\
그리고 매크로랑 함수랑 사용법 똑같게 하면 `at` 그런것까지 다 메크로로 하면 일관성 있을듯\
메크로는 함수와 연결이 된다. 메크로라는 문법이 있는게 아니라 함수에서 `mec ...\` 이런식으로 작성하게 할꺼임.

----------------------------------------------------------------------------

~~native 컴파일을 필수 말고 선택으로~~ **결정됨 → spec `None-Native` 섹션.** `using tenn-none-native;` 로 인터프리터 모드(시작 빠름) opt-in, native 가 기본. 아키텍처: 공용 프론트(lex→parse→expand→코어 IR) + 두 백엔드(LLVM native / 인터프리터). 구현 미정: 코어 IR 표기, none-native 에서의 C FFI·워커·자동양보(reduction 카운팅) 정합, 부분 native 허용 여부.

----------------------------------------------------------------------------

함수에서 국(country)을 명시적으로 지정하는 문법을 만들자. tennin 함수를 더 유연하게 — country 로 국/레벨을 직접 명시.

----------------------------------------------------------------------------

인자함수 중첩 깊이 · 포맷(들여쓰기·줄바꿈) 규칙을 초기에 확정하자. 가독성이 컴파일러가 아니라 문법에서 나오므로 포맷 규칙이 중요하다.

----------------------------------------------------------------------------

지금은 이제 함수 뒤에 바로 naming 자리에 값을 받는것도 naming 이라고 하고 있는데 이건 naming 과 근본적으로 다른 부분이다 그렇기 때문에 이것들을 naming 대신에 values 라고 하는것도 좋을것 같다.

``` tennin
switch "Luau" (
    case "Luau" ( print("Hello, World!"); break; );
    case "Python" ( print("Hello, World"); break; );
    nothing ( print("nothing"); ); @... case 중 하나도 맞는게 없을떄 실행됨
);
```

여기서 switch 뒤에 있는 "Luau" 랑 case 뒤에 "Luau", "Python" 을 values 라고 하고 구분하면 좋을것 같다.

----------------------------------------------------------------------------

실행 모델(Stack / Sub-stack / Queue) 세부를 확정하자. 큐 소유권 규칙(트리 여부), Sub-stack "독자 실행" 시맨틱(단일 흐름 중첩 스케줄링 vs 워커 물리 독립), `failed` 감독 프로토콜(실패 통지·걸린 ask reject·재시작 정책), `data` 큐 계약 표기(`queue data {}` vs `@pure` 등), Stack/큐의 물리적 실행 위치, `atomic {}`·`tick` 문법, 자동 양보 체크를 컴파일러가 심는 정확한 지점과 카운터 초기값. (zevring §11의 대응 항목 참고)

----------------------------------------------------------------------------

func 문법을 정확히 어덯게 나라를 만들고 소국을 만들고 그런것들.

----------------------------------------------------------------------------

클래스가 다른 클래스의 상속 하는거 만들기 아마도 그냥 `class 클래스 이름 extends 부모 클래스` 가 좋을듯

----------------------------------------------------------------------------

네이밍 컨벤션 통일하기.

----------------------------------------------------------------------------

타입 × Yulist × 클래스 통합 검토 (아이디어 6개 — 한 덩어리). 핵심 방향: **class 를 Yulist 로 흡수하고, would[T] 로 생성, 이름붙인 Yulist = 타입 으로 묶는다.**

1. **would 타입 메커니즘 — 확정.** would[T] 에 `.complete()` 가 생기고 실행하면 T 로 확정(봉인). 왜: 무언가를 단계적으로 세팅해 목표 형태로 만들어 갈 때 "아직 완성 안 됨 / 완성됨" 을 타입으로 드러냄 = 객체를 단계적으로 짓는 생성 원시요소. spec 타입 매직 섹션에 반영됨. (`template` 타입은 제거됨 — 생성 중 미완성 값은 would[T] 로 일원화.)
2. **이름붙인 Yulist = 타입** — py 의 `user_Pos: Point = Point(1,2,3)` 처럼 선언한 Yulist 이름을 타입 힌트로 쓰게. (타입 표 406줄 TODO "Yulist 를 타입으로 표기" 의 답이 이것.)
3. **class 제거 — 결정됨 (Luau 모델).** class 없음, Yulist(=table) + Interception(=metatable) 로 대체. 메서드 = Yulist 슬롯의 함수-값, 생성 = create 훅 + would[T].complete(), 타입 = 이름붙인 Yulist. spec `클래스 없음` 섹션에 반영. **남은 미정:** self 바인딩, Yulist 새 타입 정의 문법(6)·인스턴스 생성 문법(5). → 이것들 확정 후 개념 스케치를 실제 문법으로. (상속은 "없음 — 합성" 으로 결정: 공유 동작을 단위로 빼서 각 Yulist 가 사용. 단위 포함/위임 문법은 미정.)
   - **정리 필요:** spec·문서에 남은 `class`/`new Point`/`extends` 예시와 'comptime' 표현을 "타입도 값 / Yulist / 타입 매직" 관점으로 일괄 갱신 (모듈 섹션의 "다른 파일의 클래스" 등 포함).
4. **Yulist 선언 시 타입 매직 동작 지정** — 이 Yulist 타입으로 선언된 값을 어떻게 평가할지(condition 이 `i<3` 을 구조로 받듯)를 사용자가 정의. 유저=시스템(#9)의 실현. 메커니즘 미정.
5. **Yulist 새 인스턴스 생성 문법** 미정 (`new Point(...)` 대체).
6. **Yulist 새 타입 정의 문법** 미정.

----------------------------------------------------------------------------

C FFI 세부 (spec `C FFI` 섹션의 미정들). extern C 함수 선언 문법 + FFI 경계 블록/job 표기, 타입 매핑표(int↔int32/64, Yulist↔C 배열/struct, string↔char*, 포인터↔불투명 핸들), C→Tennin 콜백(함수 포인터 마셜링 — Tennin 함수는 naming·정보를 지녀 생포인터와 다름), struct ABI 정렬·패딩, 제로카피 pin 버퍼, C 오류(errno/반환코드) 전파, C 소유 핸들 타입 표기(opaque[T]? Cptr?), 짧음/긺 호출 경계 판정·기본값.

----------------------------------------------------------------------------

**내일 할 것 (7/25 예정):**
1. **Func 선언문 다 완성** — 다메커니즘 func 스케치를 확정 문법으로 (at countrys/country/grade/at slot, in smallCountrys/in slot/at manual, 국 내용(코드) 문법, 제너릭 자리, values 바인딩, self 바인딩). → 위 func 스케치·미정 항목들 마무리.
2. **Interception 선언문 완성** — 훅 메커니즘(값 찾기/수정/생성 등) 선언 문법 확정. (Yulist·이벤트 대체 메시징과도 연결.)
3. **모듈 만드는법 완성** — zevra id 방식, `#export`/`#module` job, id 없을 때 파일명 폴백, take 시 자동 실행 코드 표시 job. (기존 모듈 섹션 TODO 연결.)
4. **(되면) Yulist 선언법 정하기** — 새 타입 정의 문법(6)·인스턴스 생성 문법(5), 컴포넌트 포함/위임·state 문법.

----------------------------------------------------------------------------

소국을 인자 함수 **맨 위에 명시**하는 문법. 함수만 봐서는 이 함수에 무슨 소국(break/continue/특정 kind 소국 등)이 있는지 모르니까, 위에 "이 함수가 주는 소국들"을 적게 하자. (표현·문법 미정.)

-- *tenn*
