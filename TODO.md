확장슬롯을 만들자 일반적으로는 `{}:()` 가 최선의 슬롯들이다 그런데 확장 슬롯을 함수에서 사용하면 `{인자}:(매개변수):(내용)` 이런식으로도 받을수 있고 내용 대신에 **init** 이라고도 할수가 있다.\
아마도 나중에 함수 선언하는데 만들때 `{인자}:(매개변수):(init)` 이런식으로 그대로 받으면 될것 같다. 다른건 이제 이걸 뭐 `Slots.Arguments` 이런식으로 확장 슬롯의 이름을 지정하면 받을수도 있게 하면 좋을것 같다. (인자 함수 형식으로)

----------------------------------------------------------------------------

Operator 이라는걸 추가하자 아마 이건 간단히 `;`, `,`, `(`, `)`, `{`, `}`, `=`, `+` ... 이런게 좋을것 같다

----------------------------------------------------------------------------

메크로를 추가하자 그러니까 대충 함수 느낌인데 컴파일 단계에서 이게 그냥 그 함수에서 내뱉는 코드로 컴파일 되는 그런식으로 아직은 그냥 대충 개념만 생각해봄\
그리고 매크로랑 함수랑 사용법 똑같게 하면 `at` 그런것까지 다 메크로로 하면 일관성 있을듯\
메크로는 함수와 연결이 된다. 메크로라는 문법이 있는게 아니라 함수에서 `mec ...\` 이런식으로 작성하게 할꺼임.

----------------------------------------------------------------------------

native 컴파일을 필수 말고 선택으로 하게 하자 위에 `@none-native` 라고 되어 있으면 이 파일은 native 로 기계어로 변환되지 않는다는거 근데 native 로 기계어로 봐뀌는데 걸리는 시간이 진짜 얼마 안걸리면 없어도 될듯 왜냐면 이건\
시작 하자마자 바로 실행되야한는 그런거 때문에 그런거긴 함

----------------------------------------------------------------------------

tennin 에서 main 은 class 일수도 있고 func 일수도 있고 var, anything, const 일수도 있다 그리고 이게 main 이라고 표시할려면 위에 `@main` 을 적으면 된다.

거이다 완료

----------------------------------------------------------------------------

naming 을 값으로도 받게 하자. 지금은 naming 이 naming 타입만 받는데, 값·변수도 받을 수 있게 하면 `switch lang`, `case "luau"` 같은 게 특수 문법 없이 자연스럽게 나온다. naming 없는 `case` 는 기본(모두 통과)로 한다.

values 로 다시 결정됨

----------------------------------------------------------------------------

함수에서 국(country)을 명시적으로 지정하는 문법을 만들자. tennin 함수를 더 유연하게 — country 로 국/레벨을 직접 명시.

----------------------------------------------------------------------------

인자함수 중첩 깊이 · 포맷(들여쓰기·줄바꿈) 규칙을 초기에 확정하자. 가독성이 컴파일러가 아니라 문법에서 나오므로 포맷 규칙이 중요하다.

----------------------------------------------------------------------------

클래스 안 `func moveTo(...):(...)` 예시(2슬롯)를 'func 는 반환 슬롯 항상'(3슬롯) 규칙과 맞추자.

----------------------------------------------------------------------------

지금은 이제 함수 뒤에 바로 naming 자리에 값을 받는것도 naming 이라고 하고 있는데 이건 naming 과 근본적으로 다른 부분이다 그렇기 때문에 이것들을 naming 대신에 values 라고 하는것도 좋을것 같다.

``` tennin
switch "Luau" (
    case "Luau" ( print("Hello, World!"); break; );
    case "Python" ( print("Hello, World"); break; );
    nothing ( print("nothing"); ); @... case 중 하나도 맞는게 없을떄 실행됨
);
```

여기서 switch 뒤에 있는 "Luau" 랑 case 뒤에 "Luau", "Python" 을 values 라고 하고 구분하면 좋을것 같다.

----------------------------------------------------------------------------

실행 모델(Stack / Sub-stack / Queue) 세부를 확정하자. 큐 소유권 규칙(트리 여부), Sub-stack "독자 실행" 시맨틱(단일 흐름 중첩 스케줄링 vs 워커 물리 독립), `failed` 감독 프로토콜(실패 통지·걸린 ask reject·재시작 정책), `data` 큐 계약 표기(`queue data {}` vs `@pure` 등), Stack/큐의 물리적 실행 위치, `atomic {}`·`tick` 문법, 자동 양보 체크를 컴파일러가 심는 정확한 지점과 카운터 초기값. (zevring §11의 대응 항목 참고)

----------------------------------------------------------------------------

저수준에서 linked 리스트 등까지 구현할수 있게 하기 그대신 저수준 느낌이 나면 않된다. 현대 고수준 언어의 방식을 따를꺼다.\
간단히 말해서 asm 같은데에선 mov 나 push 같은걸 일일이 사용한다 하지만 tennin 는 단순하다 그냥 어떤 주소의 값 읽기, 어떤 주소 도는 바로 앞 주소같은데에 뭐 넣기\
이것만으로도 많은 구조들을 구현 가능하다. 왜냐하면 이미 tennin 의 함수는 아주 체계적이다. 그냥 새 기능을 만들려면 그에 관한 함수를 추가하면 된다.\
이 기능을 사용하기 위해 그냥 데이터 저장 / 데이터 불러 오기 등을 하는 함수를 사용하기만 하면 된다. 정말 간단하다 (Deep Module).

----------------------------------------------------------------------------

func 문법을 정확히 어덯게 나라를 만들고 소국을 만들고 그런것들.

----------------------------------------------------------------------------

클래스가 다른 클래스의 상속 하는거 만들기 아마도 그냥 `class 클래스 이름 extends 부모 클래스` 가 좋을듯

----------------------------------------------------------------------------

네이밍 컨벤션 통일하기.

-- *tenn*
