# Tennin 프로그래밍 언어 기획

> 상태: 초안 (v0.3) · 작성일 2026-07-07 · 갱신일 2026-07-21
> 구현 언어: **C++** · 목적: 실제 사용 가능한 도구

---

## 0. 대상 사용자 — 누구를 위한 언어인가

Tennin 은 **숙련된 파워 유저 — 시스템·성능 지향 개발자**를 위한 언어다. 네이티브 성능과 최대 자유도(제어구조까지 재정의)를 **둘 다** 원하고, 그 대가로 학습 곡선을 기꺼이 지불하는 사람이다. Zig·Rust·Lisp을 쓰는 부류에 가깝다.

**목적**: 다른 언어의 시그니처 기능까지 Tennin 의 체계적 함수(인자 함수)로 **간단히 재구현**할 수 있는 언어. 즉 Tennin 은 남의 좋은 기능을 흡수하는 메타적 함수 언어이며, 이것이 (생태계로는 못 이기는) 신생 언어가 파워 유저를 끌어올 유일한 해자다. 이 능력 — 다메커니즘 함수로 다른 언어의 시그니처 기능을 흡수·재구현하는 것 — 을 **Feature Absorption**이라 부른다. (근거·전략 → data.md #5)

**설계 목적**: Tennin 는 범용 언어다. 다만 첫 목적 프로그램은 게임이다 — 내가 원하는 방식으로 코드를 짜기 위해. 설계가 갈릴 때 "게임을 짤 때 어느 쪽이 필요한가"가 심판이 된다. C 가 범용이면서 Unix 를 심판으로 삼았던 것과 같다.\
Tennin 는 zevring 에서도 쓸 수 있지만 zevring 전용이 아니다 — 언어의 운명을 브라우저의 성공에 걸지 않는다. (zevring 전용 언어는 zevra 다.)

명시적으로 **아닌** 대상:

- 초보자 / 첫 언어 — 진입 장벽을 낮추는 건 후순위다(1-B #4).
- 팀 통일성을 최우선으로 두는 대규모 조직 — 자유를 닫아 두는 언어(Go 등)가 그들에게 맞다.

**왜 이걸 먼저 못박나 (판단 기준)**

"누가 쓰나"가 정해지면 앞으로의 모든 "쉬움 vs 강력함" 결정이 자동으로 풀린다. Tennin 은 충돌 시 **강력함·자유도·성능을 진입 장벽보다 우선**한다. 초보 배려용 sugar를 넣을지 고민될 때, 답은 "타깃이 파워 유저이므로 표현력·성능을 해치지 않는 선에서만"이다.

단, 가독성(1-A #2)은 여전히 중요하다 — 파워 유저용이라고 읽기 어려워도 된다는 뜻이 아니다. "자유롭게 짜되 읽기 좋게"가 목표다.

**두 마리 토끼 / 트레이드오프**

- 장점: 타깃이 명확하면 설계가 흔들리지 않는다. "자유도 vs 쉬움" 논쟁이 종결되고, 이 언어의 핵심 긴장(열린 코어 + 성능, data.md #1)도 "사용자가 그걸 원하고 감당할 수 있다"로 정당화된다.
- 단점: 대중성·초보 유입은 포기한다. 사용자층이 좁아진다. 하지만 좁고 뾰족한 정체성이 이 언어의 존재 이유다.

---

## 1. 확정된 결정

- **구현 언어:** C++
- **실행 방식:** 네이티브 컴파일러 (AOT) — 소스를 기계어 실행파일로 컴파일
- **컴파일러 이름:** CTennin 이다. C++ 느낌을 줘서 CTennin 으로 했다.
- **백엔드:** LLVM — AST를 LLVM IR로 변환, 최적화·기계어 생성은 LLVM에 위임
- **설계 원칙:** Deep Module (좁은 인터페이스 + 깊은 구현)
- **확장자:** `.tenn` 로 한다. 한눈에 Tennin 임을 알 수 있다.
- **타입 시스템** 정적으로 하되, 동적도 가능하다.
- **표준 라이브러리 범위**는 거의 모든 것을 지원하는 수준이다.
- **컴파일타임 메커니즘:** 제네릭·인자 함수 커스터마이징·상수계산·인라인이 하던 일은 expand 하나가 한다. 별도 매크로·템플릿은 없다 — C 식 텍스트 매크로가 아니라 의미 단위로 동작하는 컴파일타임 전개다. (1-A #10, 중복 금지. 자세히 → 1-C expand)
- **실행 모델:** 협력적 스케줄링 기반 계층형 액터 — Stack / Sub-stack / Queue + 메시지 전달, 필요 시 워커 승격(진짜 병렬). 공유 메모리 대신 메시지. (zevring 런타임에서 도입, 자세히 → 1-D)
- **자료 구조:** Yulist 하나로 통일한다. 배열·연결 리스트·딕셔너리는 몇 가지 세팅이 이미 된 Yulist 다. (자세히 → 1-C Yulist)
- **메모리:** end with free (국이 끝나면 즉시 소멸) + send = 자동 이주 + SM (대체 메모리). 메모리 누수 방지와 안전은 컴파일러가 진다. (자세히 → 1-C 메모리 관리)

## 1-A. 설계 철학 (구현 원칙)

> Core: 구현 복잡도를 낮추는건 다른 어떤것보다 중요하다.

Tennin 자체를 만들 때 지키는 원칙. 우선순위 순.

1. **성능이 최우선이다.** 노이즈 수준의 성능 낭비도 허용하지 않는다.
2. **가독성이 중요하다.**
3. **메모리를 최대한 절약한다.**
4. **Deep Module을 쓴다.** (좁은 인터페이스, 깊은 구현)
5. **사소한 이유로 새 추상화를 만들지 않는다.** 래퍼(wrapper) 같은 얕은
   간접층은 메모리·성능 낭비다.
6. **불필요한 간접층을 만들 빌미 자체를 제공하지 않는다.** 언어 설계가
   래퍼를 만들 이유를 만들어서는 안 된다.
7. **Jump는 최소화 해야 한다.** Jump 는 읽을 때의 흐름을 끊고 가독성을 낮춘다.
8. **파일간 Jump는 거의 없애야 한다.** 파일간 Jump 는 작업을 힘들게 한다.
9. **사용자와 시스템을 구분하지 말라.** 내장 함수 쓰는건 System.print(); 이런 식이고
   자기가 만든 함수 쓰는건 User.print(); 이런 식으로 하지 말라. 오히려 자유도를 아주 낮춘다.
10. **절대로 똑같은 기능을 여러개 넣지 말라.** 비슷한 기능 그러니까 예를 들어 키워드 추가 기능과 함수 기능
   이건 아무런 차이가 없다. 그냥 언어를 복잡하게 할 뿐이다.
   그리고 비슷한 기능은 표현지향을 사용해 구분한다.
11. **기능을 추가할 때 중복이 생기면 기능을 지우지 말고 중복이 생긴 여지를 없애라.** 기능이 추가될 때
   만약 다른 것과 겹친다면 예: `//` 를 주석으로 하려고 했는데 나눗셈의 몫을 반환하는 연산자와 겹칠 때
   기능을 제거하지 말고 아예 중복이 생길 이유 자체를 없애라 예: `//` 주석 기호가 다른 것과 겹치면 기능 제거 대신에 `@...` 을 주석으로 한다.
12. **컴파일러가 가독성을 높여주지 않는다.** 문법 자체가 가독성이 높아야 한다.

    (0xd 은 불길한 숫자라 건너뛴다 — 오타가 아니다.)

14. **언어가 특정 메커니즘을 강제하지 않는다.** 메모리 관리 방식, 에러 처리 방식 등은 언어가 하나
   로 못 박지 않고 사용자가 고른다. 강제된 메커니즘은 그것을 원하지 않는 사용자의 자유도를
   빼앗는다.
15. **언어는 유연해야 한다.** 언어는 원래 많은걸 더 쉽게 표현할 수 있어야 한다. 만약 유연하지 않으면
   가독성이 낮아진다 아주 많이 이 언어가 지원하지 않는걸 구현하기 위해.
16. **언어 컴파일러에 /\s/ 는 건너 뛴다고 되어 있어야 한다.** 애초에 들여쓰기로 구분하면 python 처럼 가독성을 올라가겠지만
   공백이 제대로 보이지 않는 markdown, gpt 같은 곳에서는 뜻이 제대로 전달되지 않을 수도 있다.
17. **안전해 보이는 코드는 안전성이 보장되고, 위험한 건 위험해 보인다.** 먼저 위험한게 안전해 보이는건 최악의 상황이다.
   위험한데 안전해 보여서 그걸 좀 실수로 잘못 바꿔서 큰일이 터질 수도 있다.
   그렇다고 좀 위험한 작업까지 다 막는건 안 좋다. 왜냐면 그러면 언어로 못하는 작업이 너무 많아 진다.
   그래서 그 두 문제의 해결책은 안전해 보이는 코드는 안전이 보장되고, 위험한건 위험해 보인다 이다.
   왜냐면 안전해 보이는건 안전이 보장되면 확실히 프로그래머는 안심하고 안전해 보이는 구간은 코드를 짤 수 있다.
   또한 위험해 보이는건 프로그래머가 직접 긴장하면서 코드를 짜게 되고 그러면 많은걸 하면서 안전하게 코드를 짤 수 있다.

또한 Tennin 는 표현 지향 Representation-Oriented-Programming ROP 과 정보 지향 Information-Oriented-Programming IOP 언어다. 무슨 뜻이냐면

먼저 표현 지향은 간단히 무엇이든지 option 으로 받지 않고 표현으로 받는것이다. 예를 들어서 이게 반복문인지를 받지 않고 그냥 이거의 종류가 반복문이라는걸 받는다.\
그러니까 그런거다. 옵션에서 선택하는 방식 그러니까 스무고개 이건 ; 으로 구분 하나요?, 이건 블럭이 {} 인가요? 이런 식으로 받지 말고 그냥 사용자가 표현을 하게 한다.\
이게 표현 지향임

정보지향은 무엇이 전달될 때 필요한 정보는 모두 보존한다. 그러니까 예를 들어서 일반적인 언어는 함수를 callback 으로 받을 때 이름은 안 받고 그냥 함수 자체를 받음 그 대신 정보지향에서는\
이 함수의 이름이 뭔지, 어디서 호출이 됐는지, 이 호출된 위치가 어딘지 그런것까지 모두 기록해서 줌

그러면 궁금할 수 있다 — 정보 지향은 메모리 낭비가 아니냐. Tennin 의 답은 **정보 굽기**다: 정보는 나르지 않고, 굽는다.

정보 지향은 두 층이다.

- **정적 층 (대부분):** 함수·값의 정보 — 이름, 선언 위치, 호출 위치, kind, 스펙 — 는 컴파일 타임에 전부 알려져 있다. 코드가 실제로 접근하는 조각만 expand 가 그 자리에 상수로 굽는다. 예를 들어 `f.name` 을 쓰면 그 자리에 이름 문자열 상수가 박히고, 접근하지 않는 정보는 실행 파일에 아예 존재하지 않는다 — 메모리 절약이 아니라 메모리 0 이다. 호출 위치처럼 호출마다 다른 정보는 expand 가 호출 지점에서 끼워 넣는다.
- **동적 층 (드묾):** 임의의 값들을 돌면서 정보를 묻는 진짜 동적 질의만, 타입마다 하나 있는 **정보표**를 참조한다. 표는 프로그램에 하나뿐이라 값마다 복사가 없고, 이 비용은 동적 질의를 쓴 코드만 낸다 (비용 정직).

그리고 "정보 지향을 쓸 거면 미리 위에 표시하면 되지 않냐"고 물을 수 있는데, Tennin 는 점프를 싫어한다. 이건 그냥 지어낸 게 아니라 python 에서 직접 import 를 했을 때 코드 흐름을 깨고 맨 위로 가서 import 를 추가하는 게 정말 귀찮았기 때문이다. 정보 굽기에서는 표시 자체가 필요 없다 — 그냥 접근하면 컴파일러가 굽는다. 위로 올라갈 일이 없다.

(이전 설계의 정보 지향 큐·val box 는 폐기한다 — 값을 넘길 때마다 큐를 만드는 비용과 요청-응답 지연이 사라진다. → CHANGELOG. 영감: Zig 의 comptime 리플렉션 계보에서 얻었다. 단, "사용하는 조각만 굽기 + 호출부 주입 + 선택적 정보표"라는 조합은 Tennin 의 expand·두 층 원칙에서 도출한 것이다.)

> 요약: 언어가 좋음이 컴파일러 말고 언어 문법에서부터 나와야 한다.

## 1-B. 실행 스타일 (언어 동작 원칙)

Tennin 언어가 사용자에게 보이는 동작 방식.


1. **콜백은 함수로만 전달한다.**
2. **제어 구조도 콜백이다.** `if`, `while` 같은 것도 특수 문법이 아니라
   함수에 넘기는 콜백(클로저)으로 다룬다. → 사용자가 자기만의 제어 구조를
   함수로 정의할 수 있다.
3. **이름은 겉모습일 뿐, 기능을 담당하지 않는다.** 이름 기반 매직(특정
   이름이 자동으로 특별하게 동작하는 것)을 금지한다. 기능은 명시적 구조로만
   결정된다.
4. **구현 자유도가 중요하다.** 자유도를 위해서라면 낮은 진입 장벽을 포기해도 된다.
5. **사용자가 언어에 기능을 추가할 수 있을 만큼의 자유도를 준다.**
6. **파일은 클래스 일 수도 있고, 함수 일 수도 있고, 다른 변수 등일 수도 있다.** 파일은 모든 형태가 가능하다
   파일은 클래스 일 수도 있다 (Java 처럼), 타입일 수도 있고, 함수로 만들 수도 있다. (자유도를 높이기 위해)
7. **함수는 1급 시민이다.** 함수가 1급 시민이 아니면 자유도가 많이 떨어진다.
   그러므로 함수는 1급 시민이다.
8. **이상한 축약어는 쓰지 않는다.** 이상한 축약어는 발음도 이상해서 정보 전달이 어렵다.
   또한 다른 축약어로 오해를 일으킨다.
   하지만 i 를 index 말고 ideas 의 축약어로 해석할 사람은 없다.
   md 를 markdown 말고 monday 로 해석할 사람은 없다.
   이렇게 축약어가 오해를 일으키지 않으면 써도 된다.

> 방향성: 최소한의 내장 문법 + 최대한의 확장 자유. 사용자가 제어 구조까지
> 직접 만들 수 있는 언어.

인자 함수·국·소국(및 특국) 체계의 핵심 이점은 **코드를 유연하게 짤 수 있다**는 것이다.

## 1-C. 문법

Tennin 언어의 사용법, 문법.

### 인자 함수

인자 함수가 필요한 이유: 다른 언어들은 블록 구분을 `{}` 로 많이 한다. 하지만 그건 일관성이 없다 (왜 그렇게 하는지도 명확하지 않다). 그래서 Tennin은 블록을 callback 형태로 넘긴다는 설정의 **인자 함수**를 만들었다. 인자 함수는 Tennin의 진짜 핵심 중의 핵심이다. Java로 치면 Class, JavaScript로 치면 `{}` 블록 구문에 해당한다.

인자 함수는 2류 시민이고, **기본형은 인자와 내용만 적는 `{인자}:(내용)`** 이다. 매개변수·반환타입 슬롯은 기본적으로 생략된다 — 매개변수는 소국으로 주입되고, 기본적으로 반환 개념도 없다.
단, 필요하면 확장 슬롯을 붙여 `{인자}:(슬롯1):(내용):(슬롯2)` 이런 식으로 해서 뭐 슬롯1 을 매개변수, 슬롯2는 반환타입 이런 식으로도 쓸 수 있다.

먼저 인자 함수를 적는 법이다 (기본형)

```Tennin
{인자들}:(내용);
```

그리고 이 `{}:(내용)` 앞에 오는 `if`, `class` 그런것들은 함수 이름이다.\
만약 `{}:(내용)` 앞에 등록되어 있지 않은 함수 이름이 오면 에러가 난다.

; 을 안적으면 그 다음에 나오는 인자 함수까지 다 같은 인자 함수로 취급 되기 때문에 `;` 이 아주 아주 중요하다.

기본형에서 매개변수는 인자 함수의 슬롯이 아니라 소국으로 주입되며(확장형에서는 매개변수 슬롯을 직접 쓸 수 있다), x :: int 처럼 타입을 명시하는 정적 매개변수가 일반적이다.

그리고 인자 함수는 여러개의 인자 함수들을 받을 수 있는데 그런 인자 함수들을 모은 인자 함수를 합국인자 함수라고 한다.

예를 들어서 `if` 문에서는 `if` 문 뒤에 `else` 도 적을 수 있게 해야 한다 그런데 `if` 뒤에 적는 인자 함수랑 `else` 뒤에 적는 인자 함수는 달라야 하지만 서로 같은 것들을 공유해야 한다.\
그래서 인자 함수에는 국이라는게 있다. (國 써서)

국들은 대충 이런 식이다.

`if` 문에서 `if` 뒤에 있는 인자 함수가 1국, `else`, `else if` 가 2국

이때 2국은 1국이 존재해야지만 쓸 수 있고 3국은 2국이 존재해야지만 사용 가능하다. 무슨 뜻이냐면

```Tennin
else (

):();
```

이건 말이 안 된다. 왜냐면 앞에 `if` 가 없는데 `else` 가 바로 왔기 때문이다.\
이렇게 `if` 는 1국이다. 그리고 else 나 else if 가 2국이다.

그리고 1국에서 2국으로 정보를 넘길 수 있고 2국에서 3국으로 정보 넘기기 그런것들이 가능하다.

그러니까

x국 에서 y국으로 정보를 보낼 때는

x <= y

이여야 한다. 즉 정보는 **낮은 국에서 높은 국으로(1→2), 또는 같은 국끼리만** 전달된다. 역방향(높은 국 → 낮은 국)은 불가능하다.

왜냐면 국의 정보는 그 국이 할 일을 끝내면 즉시 소멸하는데, 낮은 국이 높은 국보다 먼저 실행·존재하기 때문이다. 방향을 한쪽(낮은→높은)으로 고정하면, 이미 소멸했을 수 있는 정보를 읽는 일이 원천적으로 생기지 않아 "국이 끝나는 즉시 안전하게 소멸"이 성립한다.

그리고 이렇게 국끼리 정보를 주고받는것을 국간 통신이라고 한다. 국간 통신은 메모리 절약을 위해 이 국이 할일을 다 끝내면 바로 정보는 소멸된다.

- **왜:** `if`→`else`처럼 여러 블록이 서로 이어지면서(정보 공유) 각자 다른 역할을 하는 경우가 많다. 블록마다 따로 함수로 두면 서로 정보를 주고받을 길이 없고, 한 함수에 다 넣으면 역할이 뒤섞인다. 국은 '이어지는 단계'를 문법으로 드러내 이 둘을 동시에 푼다. (여러 국을 묶은 인자 함수가 위의 합국인자 함수다.)
- **장점:** 단계 순서(낮은→높은)와 정보 전달·소멸 시점이 문법으로 고정돼, 숨은 상태 공유 없이 `if`→`else` 같은 연결 구조를 안전하게 만든다. 사용자가 자기만의 다단계 제어구조도 국으로 정의할 수 있다(1-B #2·#5).
- **단점:** '몇 국인지', '어느 국에서 어느 국으로 정보가 가는지'를 알아야 해서 단순 블록보다 익힐 개념이 하나 더 늘어난다.

그리고 이 인자 함수 바로 앞, 함수 이름 바로 뒤에 오는것을 naming 이라고 하는데

```Tennin
class Point {
   at prototype (
      func moveTo(x :: int, y :: int, z :: int):(
         self.x = x or self.x;
         self.y = y or self.y;
         self.z = z or self.z;
      );
      @... 여기에는 Prototype 을 적는다.
   );
}:(self :: template, x :: int, y :: int, z :: int):(
   self.x = x;
   self.y = y;
   self.z = z;
); @... 클래스는 `{인자}:(매개변수):(내용)` 3슬롯 — 반환타입 슬롯 없음.
```

여기서는 class 가 함수 이름이고 Point 가 naming[0] 으로 들어간다.\
그리고 이 class 부분이 1국이다.

또 이 class 는 인자로는 moveTo 를 받는데 이때 moveTo 는 먼저 인자 함수를 func 라는 함수 이름으로 담고 이 moveTo 의 naming 은 (moveTo) 라는 배열로 반환된다.

- **왜:** `class Point`의 `Point`, `func moveTo`의 `moveTo`처럼 '함수 이름 뒤에 붙는 이름'은 값도 매개변수도 아닌 제3의 것이다. 이를 인자나 매개변수 자리에 욱여넣으면 '이름'과 '데이터'가 섞여 의미가 흐려진다. naming은 이 이름표를 전용 자리로 빼서 명확히 다룬다.
- **장점:** 인자(`{}`) 자리로 받기엔 어색한 것 — 이름표처럼 값·데이터가 아닌 것 — 도 naming으로는 쉽게 받을 수 있다. 인자 슬롯을 오염시키지 않고 전용 통로로 받는 셈이다. 또 이름과 실제 인자·매개변수가 섞이지 않아 읽기 쉽고, `naming[0]`처럼 배열로 접근해 이름이 여러 개여도 규칙적으로 다룰 수 있다. '이름은 겉모습일 뿐 기능을 담당하지 않는다'(1-B #3)와도 맞물린다 — 이름은 naming이라는 데이터일 뿐 특별한 동작을 하지 않는다.
- **단점:** naming이라는 개념을 하나 더 배워야 하고, 이름이 값처럼 보이지 않아 처음엔 어디로 들어가는지 헷갈릴 수 있다.

그러니까 인자 함수는 함수를 호출할 때 넣는 인자다.

tmi
용어가 좀 이상할텐데 원래 이게 함수랑은 많이 떨어져 있다고 생각할 것 같다. 사실 처음에는 함수랑 좀 가까웠는데 좀 더 체계적으로 하다 보니까\
함수랑은 좀 거리가 멀어졌다.

### 원칙 0 — 모든 구문은 인자 함수의 축약이다

Tennin의 모든 것(리터럴, 값, 제어구조, 클래스)은 단 하나의 형식 — **인자 함수** — 의 특수 케이스이자 축약이다. 기본형은 `{인자들}:(내용)` (3형)이고,\
필요하면 확장 슬롯을 붙여 `{인자}:(슬롯1):(내용):(슬롯2)` 이런 식으로 해서 뭐 슬롯1 을 매개변수, 슬롯2는 반환타입 이런 식으로도 쓸 수 있다.

- 리터럴 `5` 는 `{5}:()` 와 같다. (값 `5`가 인자로 전달되는 것.)
- 리터럴은 내용 슬롯이 없는 특수 인자 함수다. 박아둘 코드가 없으므로 어디로든 갈 수 있어 1급이다. 1급/2류의 경계선 = 내용 슬롯을 갖는가.
- `if`, `class`, `while` 등은 이 구조 앞에 오는 "함수 이름"일 뿐이다. 특수 문법이 아니다. (등록되지 않은 함수 이름이 오면 에러.)

**왜 이렇게 하나 (WHY)**

언어에 구문이 사실상 하나뿐이면 배울 것도 하나이고 중복도 원천적으로 없다. 이는 1-A #10(비슷한 기능 중복 금지)의 극단적 실현이며, 1-B #2·#5(제어구조도 콜백, 사용자가 제어구조를 직접 정의)를 완성한다. 사용자가 새 함수 이름을 등록하면 `if`처럼 쓰는 자기만의 제어구조를 만들 수 있다.

**두 마리 토끼 (장점 vs 단점)**

- 장점: 하나의 형식만 익히면 언어 전체가 열린다. 파서·문법이 단순해지고, "이건 문법이고 저건 함수" 같은 이질감이 사라진다(1-B #7, 함수 1급 시민).
- 단점: 값 하나 쓰자고 `{5}:()`를 매번 칠 순 없다. → 그래서 자주 쓰는 형태에 **축약형(1·2형)**을 둔다. 대신 축약 규칙을 익혀야 하는 학습 비용이 생긴다.

**성능 (expand)**

if/while 같은 핵심 제어는 expand 로 정의된다. expand 는 호출을 컴파일 타임에 전개하므로 콜백 호출 비용이 0 이다. 열린 코어를 유지하면서 네이티브 성능을 지키는 것, 이 승부처의 답이 expand 다.

### 인자 함수의 형태

인자는 `;` 로 구분한다. (인자는 **함수 쪽 경계**이므로 `;`. 배열·리스트 같은 **데이터 원소 경계**는 `,` 로 축이 다르다. TS에서 함수 매개변수와 리스트가 다른 것과 같다.)

- **1형** `"아아"; "비비"` — 값들. 값이 인자로 전달된다. (`{"아아"; "비비"}:()` 의 축약)
- **2형** `("아아"; "비비")` — 괄호로 묶은 값들(`;`로 구분). 인자로 전달된다.
- **3형 (기본형)** `{인자들}:(내용)` — `{}`=인자들, `()`=내용. 인자 함수의 기본 형태. 매개변수·반환타입 슬롯은 기본적으로 생략된다(매개변수는 소국으로 주입, 기본적으로 반환 개념 없음).

**왜 1·2형이 필요한가**: 기본 형태는 3형 `{인자들}:(내용)` 이다. 하지만 값만 넘길 때 `{"1"}:()` 처럼 매번 쓰는 건 낭비이므로(→1형), 값을 소괄호로 묶어 넘기는 축약(→2형)을 둔다. 즉 1·2형은 3형의 축약이다.

**1형 vs 2형 규칙**: 1형은 `()`로 묶이지 않아 단순할 땐 간결하지만, 인자가 복잡해지면 가독성이 떨어지고 컴파일러가 잘못 해석할 위험이 있다. **따라서 복잡한 경우에는 반드시 2형(`( ; )`)을 써야 한다.** 1형과 2형은 의미가 같고, 2형이 안전·명시형이다.

**기본형 vs 옵션 확장형**: 기본형은 `{인자}:(내용)` 이지만, 원하면 내용 앞에 매개변수, 뒤에 반환타입을 붙여 전체형 `{인자}:(매개변수):(내용):(반환타입)` 으로 쓸 수 있다. (내용 앞 소괄호 = 매개변수, 뒤 소괄호 = 반환타입.) 이 확장 슬롯을 정확히 어떤 규칙으로 받고 처리할지는 `func` 설계 시 확정한다 (→ 미정 사항).

- **왜:** 대부분의 콜백(예: `if`/`while`에 넘기는 블록)은 매개변수도 반환도 필요 없다. 기본을 생략형으로 두면 가장 흔한 경우가 가장 짧게 써진다. 그렇다고 슬롯을 완전히 없애면 `func`·`class`처럼 실제로 매개변수를 받고 값을 돌려줘야 하는 경우를 표현할 수 없으므로, 슬롯을 지우지 않고 옵션으로 남긴다.
- **장점:** 흔한 경우는 간결하고(생략형), 필요한 경우는 표현력을 잃지 않는다(확장형). 한 문법으로 두 마리 토끼를 잡는다.
- **단점:** 같은 인자 함수인데 슬롯 개수가 달라질 수 있어, 읽는 사람이 '이건 몇 슬롯인가'를 구분해야 한다. 이 구분·파싱 규칙을 `func` 설계에서 확실히 못박지 않으면 모호해질 수 있다(그래서 규칙을 미정 사항으로 명시한다).

### 값이 나오는 경로

- 1·2형은 **인자 자체가 값**이다. (`5` → 5)
- 기본형 인자 함수에는 반환 개념이 없다. 즉 반환타입 슬롯을 켜지 않는 한 `return`으로 값을 내보내는 경로가 없다.
- 반환은 확장형에서 반환타입 슬롯을 붙였을 때만 의미를 가지며(대표적으로 일반 함수), 그 정확한 명시·처리 방식은 함수 구조(`func`) 설계 시 확정한다(→ 미정 사항).

**왜**: 기본형의 값 경로를 인자(1·2형)로만 고정하면, 숨은 반환·숨은 제어흐름이 생기지 않는다. 반환이 필요한 경우에만 슬롯을 명시적으로 켜므로 반환 경로가 항상 코드에 드러난다(원칙 3).

### 국 / 소국 (小國)

**국 = 명시적 스코프/아레나**: 국은 정보 전달과 즉시 소멸이 문법에 드러나는 명시적 스코프(아레나)다. 국이 할 일을 끝내면 정보는 즉시 소멸한다. 숨은 제어흐름도, 숨은 할당도 없다(원칙 3).

**소국(小國)**이란 특정 국 또는 인자 함수 안에서만 존재하는 존재다(문맥 종속). 함수일 수도, 변수일 수도, 클래스일 수도 있다 — 무엇이든 될 수 있다.

- 반복문(`for`/`while`)의 국 안에는 `break`, `continue` 소국이 있다.
- `continue`는 while 안에 새 국을 여는 소국으로 취급한다.

- **왜:** 어떤 기능(예: `break`, `continue`)은 오직 특정 문맥(반복 국) 안에서만 의미가 있다. 이런 것들을 전역 함수로 두면 엉뚱한 곳에서 호출돼 에러가 나기 쉽다. 소국은 '이 국 안에서만 존재'를 문법으로 보장한다.
- **장점:** 문맥에 맞지 않는 호출이 애초에 불가능해져(스코프로 차단) 실 수가 준다. 또 국이 끝나면 함께 소멸하므로 메모리도 절약된다(원칙 3).
- **단점:** 같은 이름이라도 어느 국 안이냐에 따라 존재 여부가 달라져, 처음 보는 사람은 '이게 여기서 되나?'를 문맥으로 판단해야 하는 학습 부담이 있다.

**특국(特國)**: 연산자(`rshift`, `or`, `^` 등)를 담당하는 특수한 국. 두 가지 점에서 특수하다 — (1) 특수 기호를 이름으로 쓸 수 있다, (2) 소국과 달리 문맥에 구애받지 않고 **모든 인자 함수에서 사용 가능**하다. 즉 국은 단계, 소국은 문맥 종속 함수, 특국은 어디서나 쓰는 기호 연산이다.

- **왜:** 연산자(`+`, `^`, `or`, `rshift` …)는 코드 어디서나 쓰이고, 매번 국 문맥을 따지게 하면 너무 불편하다. 그래서 연산자는 '어디서나 쓰는 기호 함수'라는 특별한 국(특국)으로 따로 묶었다.
- **장점:** 연산자를 문맥 제약 없이 자유롭게 쓰면서도 '이름은 기능을 담당하지 않는다'(1-B #3)는 원칙을 지킨다 — 특수 기호도 결국 등록된 함수(특국)일 뿐이다. 사용자가 자기 연산자를 특국으로 추가할 여지도 열린다.
- **단점:** 특수 기호를 이름으로 허용하므로 기호가 서로 겹칠 위험이 있다(예: `//` 주석 vs 몫). 이는 1-A #11(중복이 생길 여지 자체를 없앤다)로 관리해야 한다.

**국 실행 순서 + break**: 국은 번호 순으로 실행된다 — 1국 먼저, 그다음 2국. 함수 선언 안의 소국에서 `break`를 쓰면 '이번 나라(현재 국)' 또는 '여기까지만' 실행하고 중단할 수 있다. (이는 break=소국+level 규칙과 연결되며, level이 국을 센다.)

- **왜:** `if`→`else`처럼 뒤 국은 앞 국의 결과에 기대는 경우가 많다. 실행 순서를 번호대로(1국→2국) 고정하면, 앞 국이 먼저 끝나 정보를 넘기고 소멸하는 흐름이 자연스럽게 성립한다(국간 통신의 낮은→높은 방향과 일치).
- **장점:** 순서가 예측 가능해 읽기 쉽고, '앞 국이 이미 끝났다'가 보장되므로 정보 소멸 시점도 안전하게 정해진다.
- **단점:** 국 순서가 고정이라 뒤 국을 먼저 실행하는 식의 자유로운 재배치는 불가능하다(대신 그런 자유는 대부분 필요 없고 혼란만 준다).

**같은 번호 국의 반복**: 같은 번호의 국이 여러 개면 (예: else if 반복) 적은 순서대로 실행된다. 국간 통신도 그 순서를 따라 릴레이된다: 1국 → 첫 2국 → 둘째 2국 → …. else if 체인의 "앞에서 이미 실행됐으면 건너뛰기"가 이 릴레이 (taken 전달) 로 정의된다.

### return / break / continue = 소국 + level

- `return`·`break`·`continue`는 모두 소국이다.
- 셋 다 `level` 인자를 받는다. `level 0`이 기본값(가장 가까운 대상), 값이 클수록 더 바깥을 가리킨다.
- **level을 세는 단위가 다르다**:
  - `return`의 level은 **반환을 가진 일반 함수만** 센다. (일반 함수의 반환 방식은 함수 구조 설계 시 확정)
  - `break`/`continue`의 level은 **반복 국만** 센다.
- 이로써 로컬 탈출과 비로컬 탈출이 `level` 하나로 통합되고, 비로컬 탈출도 항상 코드에 숫자로 명시된다 → "숨은 제어흐름 없음"(원칙 3)을 만족한다.

**소국에 인자 넘기기**: `break`/`continue`/`return`에 여러 값과 level을 함께 넘길 때는 인자만 받는 `()`형을 쓰고 명명 인자를 허용한다. 인자는 `;`로 구분하며, 순서 기반과 명명 기반(`level = 1`) 둘 다 가능하다.

예: `return (values = (1, 2); level = 1);`

- **왜:** 다른 언어는 로컬 반환(`return`)과 비로컬 탈출(라벨 `break`, 예외 등)을 제각각 다른 문법으로 처리해 규칙이 늘어난다. Tennin은 셋 다 '소국 + `level`'이라는 하나의 모델로 통합한다.
- **장점:** 배울 개념이 하나로 줄고(중복 금지, 1-A #10), 얼마나 바깥으로 나가는지가 `level` 숫자로 코드에 항상 드러나 숨은 제어흐름이 없다(원칙 3).
- **단점:** `level` 숫자를 직접 세어야 하고, 중간에 국이 추가·삭제되면 숫자가 어긋날 수 있다. 그래서 큰 `level`을 남발하면 오히려 읽기 어려워질 수 있다.

### 타입

| 타입    | 설명                            |
|--------|---------------------------------|
| int    | 64 비트 정수                     |
| double | 배정밀도 실수                     |
| regex  | 정규식                           |
| naming | 무엇의 이름을 표기할 때 사용        |
| string | 문자열                           |
| char   | 문자                             |
| boolean | true / false                   |
| template | 클래스 만들 때 처음 self 의 틀     |
| condition | 조건                           |
| Yulist   | 그냥 아무런 Yulist, 유일한 자료 구조라 PascalCase 를 쓴다. |
| void   | 아무것도 없는것                    |
| nothing   | nothing                       |
| NaN    | 숫자가 아닌데 숫자                 |
| never  | 값이 진짜 없음                     |
| byte[타입]   | 8비트 그 타입                |
| short[타입]  | 16비트 그 타입               |
| middle[타입] | 32비트 그 타입               |
| decimal[타입] | 숫자 타입을 10진수 형태로 저장 |
| typeof[타입]  | 그 타입 자체 타입 안넣으면 그냥 아무런 타입 |
| would[타입]   | 지금은 그 타입이 아니지만 그 타입이 될것이다. |

일부 타입 제외 모든 타입은 64 비트를 소모한다.

> 타입 이름은 camelCase(소문자 시작)를 원칙으로 하되, `NaN`은 예외다. Not a Number 는 표준적으로 `NaN`으로 표기하고 그 표기가 훨씬 익숙하기 때문이다. (익숙함이 규칙 일관성보다 이득인 유일한 예외로 둔다.)

뒤에 ? 가 붙으면 nothing, NaN, void 또는 never 일 수도 있다는 것이다.

naming 은 특이한 타입인데 "" 로 감싸지 않고 바로 온다. 인자 함수의 naming 들이 이 `(...naming)` 이런 형식으로 온다.

// TODO: Tennin 의 복잡한 인자 함수 기반 함수 시스템을 어떤 식으로 타입으로 표현할지 생각하기.
// TODO: Tennin 의 Yulist 를 어떤식으로 타입으로 표기하게 할지 생각하기

### 타입 매직

Tennin 에서 이름은 기능을 담당하지 않는다 (이름 매직 금지, 1-B #3). 대신 타입이 기능을 담당한다 — 이것이 **타입 매직**이다.

**값의 평가 방식은 스펙에 선언된 타입이 정한다.**

| 선언된 타입 | 받는 것 |
|---|---|
| (기본) | 평가된 값 — `i < 3` → `true` |
| condition | 식의 구조 — `i < 3` → first = i, operator = Operator.lessThan, second = 3 |
| naming | 이름 그 자체 — `VAR` → VAR 라는 이름. 같은 이름의 변수가 있어도 이름이 이긴다 |

또 

condition 세부:

- 필드는 값이 아니라 참조다 — 재평가가 가능하다. (`ref(< condition.first = i; condition.second = 3; >);` 모델)
- 복잡한 식은 condition.equations 로 부분식 리스트를 얻는다 (`(...condition)` 타입).
- 단락: equations 는 왼쪽부터 평가한다. and 는 첫 거짓에서, or 는 첫 참에서 멈춘다. (`x <> 0 and 10 ~/ x > 1` 의 0 나눗셈 방지가 여기에 걸린다.)
- 성능: expand 문맥에서 구조는 컴파일 타임에만 존재하고, 런타임엔 기계어만 남는다. 구조를 런타임에 해석하면 Smalltalk 식 감속이다 — 이 문장이 성능 방어벽이다.
- 탈출: condition 은 자기 국의 변수를 품는다 — 클로저와 같은 취급. 국 밖으로 나가는 경로는 send 뿐이고, send 하면 캡처된 변수가 함께 이주한다. 이주는 그 시점의 스냅샷이다.
- 정직한 단점: 호출부 (`while {i < 3}`) 만 봐서는 캡처 여부가 보이지 않는다 — 스펙이 정보원이다. 대신 호출부가 항상 깨끗하다. (`while condition (i < 3)` 같은 명시 표기는 더 복잡해져서 기각.)

왜 이름 매직은 금지고 타입 매직은 원리인가: 이름은 겉모습이고, 타입은 선언된 구조다. "기능은 명시적 구조로만 결정된다"(1-B #3) 를 타입 매직이 그대로 지킨다.

(선례: C# 은 람다를 Expression 타입에 대입하면 코드 대신 식 트리가 잡힌다 — LINQ 가 그 위에 서 있다. naming 캡처는 C# 의 nameof, Lisp 의 quote 계보. 타입 선언만으로 정해지는 것이 Tennin 식이다.)

함수를 표기하는 법은

이다.

타입 앞에 ... 이 오면 그 타입의 값들이 여러개 라는 뜻이다.\
예시: ...any, ...boolean, ...regex

Yulist (배열·연결 리스트·딕셔너리 전부 — → Yulist 섹션) 의 타입 표기는 `Yulist[...]` 다.

```Tennin
Yulist[init = ...]
```

이전의 `( )` (배열), `[ ]` (연결 리스트), `{ 키: 값 }` (딕셔너리) 세 갈래 표기는 폐기한다 (→ CHANGELOG). 왜냐면 타입 표에 이미 `byte[타입]`, `typeof[타입]` 이 있다 — **대괄호 = 타입 매개변수**라는 축이 이미 있었고, `Yulist[...]` 는 그 축에 올라탄다. 오히려 세 갈래 표기가 그 축에서 벗어난 예외였다.

정직한 비용: 제일 흔한 타입이 길어진다 (`Yulist[...string]` vs 이전의 `(...string)`). 파워 유저 타깃이라 일관성이 이긴다.

### 리터럴

- **정수**: `1 2 3 4 5 ...`
- **실수**: `1.2d`, `1.3d` — `d` 접미사는 double 리터럴을 뜻한다. `d` 는 double 약자이므로 double 에만 쓴다 — 단정밀도가 필요하면 `middle[double]` 로 캐스팅한다. 받는 타입이 있으면 접미사보다 타입 매직이 이긴다 (예: `var x :: middle[double] = 0.5d` → 32비트).
- **문자열**: `"..."`또는 `«...»`
- **문자**: `'.'`
- **nothing**: `nothing`
- **NaN**: `NaN`
- **참 / 거짓**: `true` / `false`

\
문자열 포매팅: `` `...` `` 여기서 값을 넣으려면 `` `({...})` `` 이다. 원래 `{}` 만 사용하면 {} 를 사용하려면 `{{}}` 이렇게\
해야한다. 그러면 보기도 불편하고 이스케이프 지옥이 된다.\
그래서 `{}` 대신 `({})` 으로 했다. 만약 안에서 `({` 이나 `})` 를 사용하려면 각각\
`\fl` (format left) 로 `({` 가 가능하고 `\fr` (format right) 으로 `})` 가능하다.

또한 문자열에 이상한 기호 `«...»` 가 있는 이유는 그냥 독창성을 위해서가 아니라 \sq, \dq 그런 이스케이프 지옥을 막기 위해서 한거다 참고로 따옴표나 쌍따옴표가 얼마 안 들어가면 그냥 이스케이프 쓰는걸 권장하고\
뭐 진짜 긴 문자에서 " 나 ' 가 많이 사용될 때 «» 를 쓰는걸 권장한다.

// TODO: %d 와 %s 등의 포매팅 그냥 Tennin 는 %d, %s 그런거 헷갈리니까 %f 로 통일하는게 좋을것 같다.


```
2진수:  0b 예: 0b100101
8진수:  0o 예: 0o242322
16진수: 0x 예: 0xffffff
```

16 진수에서 대문자로 0xAB 나 대소문자 섞어서 0xAb 같은것도 가능하다.

지수: 숫자e지수

이스케이프:

```
'\n'                                // 줄바꿈
'\t'                                // 탭
'\r'                                // 캐리지 리턴
'\\'                                // 백슬래시
'\q' (quote ' 의 줄임말)             // 작은따옴표
'\qq' (quote quote 의 줄임말 ''      // 큰따옴표
그러니까 " 쌍따음표다)
'\u유니코드'                         //유니코드
```

이때 유니코드는 '\u{유니코드}' 도 가능하고 '\u{유니코드}' 로 하면 장점은 유닛코드를 숫자로 표시해서 '\u{0xab}' 같은것도 가능하다.
그리고 이진수 표현 '\u{0b1011}' 이나 '\u{0o257}' 이런것도 가능하다. 그냥 숫자면 가능하다. 그래서 연산이나 그런게 있을땐 '\u{유니코드}' 를 사용하는걸 권장한다.

여러줄:

```
"""
내용
"""
```

그리고 숫자 안에 _ 를 넣을 수 있다.

예시:

1_000_000\
2_321_424

좀 더 쉽게 숫자를 표현 가능하다.

정규식:

`/[a-zA-Z0-9_가-힣ㄱ-ㅎㅏ-ㅣ]/`

> 일부 리터럴 제외 camelCase 를 쓴다.

### 연산자

연산자는 특국(特國)으로 분류된다. (→ 1-C "국 / 소국" 참고: 특수 기호를 이름으로 쓰며, 문맥에 구애받지 않고 모든 인자 함수에서 쓸 수 있다.)

OR, AND, NOT : or 은 `or` 을 사용하고 and 는 `and` 를 사용하며 not 은 `not` 을 사용한다.
비트 : 비트 AND 는 `bitand` 비트 OR 은 `bitor` 비트 XOR 은 `bitxor` 비트 not 은 `bitnot`

더하기는 `+` 빼기는 - 나누기는 `/` 곱하기는 `*` 이다.\
나눗셈의 나머지는 % 나눗셈의 몫은 `~/` 다.\
제곱은 `^` 을 사용한다. `x ^ y` 일 때 x, y 는 숫자 (int·double) 면 된다. (TODO: double 지수의 결과 타입 규칙)

같다는 `==` 고 타입변환까지 해서 같다는 `~=` 다.\
같지 않다는 `<>` 고 타입변환까지 해서 같지 않다는 `<~>` 다.\
크다는 `>`, 작다는 `<`, 크거나 같다는 `>=`, 작거나 같다는 `<=` 다.

같지 않다를 `<>` 로 했을 때 이런 의문이 들 수도 있다. `<>` 는 다르다는 의미가 안 나지 않냐, 먼저 `<>` 를 다르다는 의미로 쓰는 언어는 아주 많다.\
대표적으로 SQL, Pascal 이 있다. 또한 ! 는 다르다는 느낌을 주지 않는다고 느껴진다. 왜냐하면 ! 는 강조 느낌이지 다르다는 느낌을 받은적은 얼마 없기 때문이다.\
또한 난 `<>` 가 불일치 느낌을 줘 다르다는 느낌을 준다고 생각한다.

우측 쉬프트는 `rshift`, 좌측 쉬프트는 `lshift` 이다.

### 연산자 우선순위

높은 것부터:

1. `( )` 괄호
2. `^` — **우결합** (수학 관례: `2 ^ 3 ^ 2` = `2 ^ (3 ^ 2)`. 그리고 `-2 ^ 2` = `-(2 ^ 2)` = -4)
3. 단항: `bitnot`, 부호 `-`
4. 곱셈군: `*` `/` `~/` `%` `lshift` `rshift` `bitand`
5. 덧셈군: `+` `-` `bitor` `bitxor`
6. 비교: `==` `~=` `<>` `<~>` `>` `<` `>=` `<=`
7. `not`
8. `and`
9. `or`

`^` 만 우결합, 나머지는 전부 좌결합이다. 비교 연쇄 (`a < b < c`) 는 컴파일 에러다 (boolean 과 int 의 비교가 되므로 정적 타입이 자연히 잡는다 — 명시만 해 둔다).

왜:

- and 가 or 보다 위다 — 불 대수 관례 (and = 곱, or = 합). 같은 순위면 `a or b and c` 가 Tennin 에서만 다르게 읽혀 모두가 놀란다.
- 비트 연산이 수식군에 있다 — C 가 40 년 후회한 배치 (`x bitand mask == 5` 함정) 를 Go 가 고친 방식이다. Tennin 는 정적 타입이라 침묵 버그 대신 에러가 나긴 하지만, 괄호를 강제당하는 불편은 남으므로 수식군에 둔다.
- bitnot 은 단항이라 위에 있고, not 은 Python 처럼 비교 아래다 — `not a == b` 가 `not (a == b)` 로 읽히는 것이 자연스럽다.
- and/or 가 최하위라서 condition.equations 의 최상위 분해층이 된다 — 타입 매직과 정합.

// TODO: 연산자들 더 확장하기.

### 주석

먼저 주석은 보기 쉽게 `@...` 이다 @ 가 강조 느낌을 주고 ... 은 흘리는 느낌을 준다 또 `#...` 그런것 보다 `@` 가 더 진하고 그렇기 때문에 주석 느낌을 많이 준다\
그리고 // 는 입력하려면 좀 손가락이 애매하게 위치된다 하지만 @ 는 2 아주 편안한 위치다 Shift 도 많이 사용하기 때문에 불편하진 않을것이다.\
또 . 은 손이 좀 애매해지긴 하지만 그래도 익숙하면 쓰기 많이 편하다. 여러줄 주석은 보관함 느낌을 주기 위해\

```Tennin
@<<
   오늘 할 거:
   TODO 좀 완성하고
   구조 설계좀하기
>>
```

를 사용한다.

기대 출력 주석이라는것도 있는데

아마도 이건 이게 결과일꺼야 하는 주석이다.

`@=` 가 기대 출력 주석인데 예시만 보여주자면

```Tennin
print(5) @= 5
```

이게 기대 출력 주석의 쓰임새의 예시다. 또는

```Tennin
if {5 == 5}:( @= true 기 때문에 실행됨
   print(5) @= 5
);
```

이런 식으로 쓰일 수도 있다.

### 변수, 상수, 동적 변수

`var`, `anything`, `const`는 '특수 함수(Keyword)'로 분류한다 — 좀 특이한 방식으로 동작하는 함수다.

- **왜:** `var`/`const` 같은 선언을 언어에 박힌 '특수 키워드'로 두면 사용자가 만든 함수와 내장 문법이 갈라진다(1-B #3·#9 위반). Tennin은 이들도 결국 함수의 일종('특수 함수')으로 본다 — 다만 변수를 만드는 등 좀 특이하게 동작할 뿐이다.
- **장점:** '내장 문법 vs 사용자 함수'의 경계가 사라져 일관성이 높아지고, 원리상 사용자도 비슷한 선언 함수를 정의할 여지가 생긴다(자유도).
- **단점:** 겉보기에 키워드처럼 생겼는데 실은 함수라, 처음 보는 사람은 '이게 왜 함수지?'라는 개념적 간극을 한 번 넘어야 한다.

먼저 변수를 만드는 방법이다.

```Tennin
var x :: int = 1;
var p :: string = "Hello, World";
```

변수 이름이 있는 곳이 모두 정렬 되어 있고 타입도 정렬되어 있어 보기 편하다.

``` java
int x = 1;
string p = "Hello, World"
```

이런 식으로 다른 언어는 변수 이름 위치가 들쭉날쭉 하기 때문에 보기 힘들다.

그리고 만약 동적을 원한다면

```Tennin
anything x = 1;
anything p = "Hello, World";
```

구조다

var와 글자 수 차이를 줘 일반 변수 (정적 변수) 랑은 다른 느낌을 준다.

```Tennin
var x::int = 1;
```

도 가능하다 가독성을 위해

```Tennin
var x :: int = 1;
```

형태를 권장한다.

상수 만드는 법도 간단하다.

```Tennin
const x :: int = 1;
@... 가독성을 위해 `const x :: int = 1;` 이런 형태 권장
const y::int = 2;
```

이것도 마찬가지고 var, anything 과 글자 수 차이를 줘 다른 느낌을 준다.
const 는 타입이 필수다. 그리고 타입으로 any 도 가능하다.

그리고 anything 이든 var 이든 const 든 다 1급 시민이다.

그리고 anything 이나 var 를 변경하는 법이다.

```Tennin
var x :: int = 1;
x = 2;
```

이렇게 var 는 간단하게 값을 바꿀 수 있다. 그리고 anything 동적 변수는

```Tennin
anything x = 1;
x = 2 :: int; @... anything 동적 변수를 바꿀 때는 무슨 타입으로 바꾸는지 적어놔야한다.
```

또한 타입이 any 인 변수랑 anything 이랑은 엄연히 다르다.

먼저 타입이 any 인 변수는 바꿀 때 무슨 타입으로 바꾸는지 안 적어도 되고,\
anything 은 값을 바꿀 때 무슨 타입으로 바꾸는지 적어야 한다.

그리고 결정적인 차이는 컴파일러가 타입이 any 인 변수는 아예 그냥 타입이 any 라는 정적 변수로 보고\
anything 은 그냥 동적 변수로 본다.

간단히 정리해

#### 변수 (정적):
> var [변수 이름] :: [변수 타입] = [변수 값];

#### 동적 변수:
> anything [동적 변수 이름] = [동적 변수 값];

#### 상수:
> const [상수 이름] :: [상수 타입] = [상수 값];

네이밍 컨벤션:

#### 변수 (정적)
> PascalCase

#### 동적 변수:
> camelCase

#### 상수:
> camelCase 또는 SCREAMING_SNAKE_CASE

그리고 Tennin 는 변수라고 하면 정적 변수고 동적 변수는 따로 동적 변수라고 한다.\
정적 변수는 엄격한 느낌을 줘 PascalCase 를 선택했고.

동적 변수는 살짝 느슨한 느낌을 줘 camelCase 를 선택했고

상수는 상수 특유의 느낌을 위해 SCREAMING_SNAKE_CASE 나 camelCase 를 선택했다.

### 함수

**함수의 두 쓰임 (용어).** 같은 인자 함수를 어떻게 쓰느냐로 둘로 부른다. **다른 메커니즘이 아니라 쓰임의 구분일 뿐이다.**

- **쓰리 메커니즘 함수** — 매개변수·내용·반환, 세 슬롯만 쓰는 단순 함수. (func는 반환 슬롯을 항상 쓰므로 void여도 세 슬롯 → 언제나 정확히 셋이라 "쓰리"가 딱 맞는다.) 예: `greet(name)`.
- **다메커니즘 함수** — 여기에 국·소국·values 등 추가 메커니즘까지 받아 다루는 체계적 함수. 예: `if`, `while`, `switch`, `special`.

(`일반화`/`구체화` 같은 이름은 CS의 제네릭·특수화와 충돌해 쓰지 않는다. 표기는 표준 맞춤법 '메커니즘'으로 통일.)

그리고 절대로 **쓰리 메커니즘 함수** 정의 방법이 Tennin 의 복잡한 **다메커니즘 함수** 의 정의 방법에 의해 복잡해지면 안 된다. 만약 그렇다면 결국 Tennin 는 굳이 필요 없는것까지 복잡하게 만드는 언어가 된다.

그리고 함수 선언하는 법이다.

먼저 func 를 사용한다. func 를 사용해 함수를 만들 수 있다.

```Tennin
func ...
```

또한 func 뒤에는 함수 이름이 온다.

```Tennin
func helloWorld ...
```

/*
TODO: 함수 FuncKind 를 다시 적어 놓기 FuncKind 는 이 함수의 종류인데 kind 고 FuncKind.LOOP 하면 반복문임 또 FuncKind.RETURN 은 반환만 하는거고 FuncKind.DOING 은 뭘 하는거임, 그리고 OOP 로 FuncKindSmallCountry 라고 해서 함수종 소국이라는게 있는데\
딱 그 종류의 함수에만 자동 추가되는 함수임, FuncKind.add 할때 같이 추가함
*/

### 클래스

클래스를 만드는 법은 아주 간단하다. `class Point` 이거면 된다. 상속은 `class Point3D extends Point` 이렇게 하면 된다.

클래스 선언은 인자 함수의 **확장 슬롯을 쓰는 대표 사례**다. 형식은 `{인자}:(매개변수):(내용)` — 즉 매개변수(클래스가 받는 것)와 내용(클래스 본문)을 쓰고, **반환타입 슬롯은 없다**. (`{}` 안 인자 = 프로토타입/멤버, 첫 소괄호 = 클래스가 받는 매개변수, 둘째 소괄호 = 본문.)

- **왜:** 클래스는 생성 시 인자를 받아야 하므로 매개변수 슬롯이 필요하지만, 클래스 자체가 값을 '반환'하는 개념은 어색하다. 그래서 매개변수는 켜고 반환타입은 끈 3슬롯 형태로 둔다.
- **장점:** 흔한 콜백은 기본형으로 짧게, 클래스처럼 매개변수가 필요한 경우는 같은 문법의 확장형으로 자연스럽게 표현된다.
- **단점:** 생성·타입 처리 등 세부 규칙이 아직 확정 전이라, 아래 예제의 일부 동작은 미정이다(TODO 참고).

여기에 넣는 인자 함수는 이런 형식이다.

```Tennin
class Point {
   at prototype (
      @... 여기에는 Prototype 을 넣는다.
      @... TODO: func 문법 확정 후 이 예제의 func 표기 갱신
      func moveTo(x :: int?, y :: int?):(
         self.x = x or self.x;
         self.y = y or self.y;
      );
      @... 여기에 넣은 인자 함수들은 나중에 func 1급 시민으로 변환되어 class 에 추가된다.
   );
}:(self :: template, x :: int, y :: int):( @... 받는 매개변수.
   self.x = x;
   self.y = y;
   @... 여기에는 init 시작했을 때 하는걸 넣는다. 나중에 Point.init 으로 1급 시민으로 변환된다.
); @... 클래스는 반환타입 슬롯이 없다 — 매개변수 + 내용(3슬롯)만 쓴다.
```

상속 같은거 하고싶으면

```Tennin
class Point3D extends Point {
   at prototype (
      @... TODO: func 문법 확정 후 갱신
      func moveTo(x :: int?, y :: int?, z :: int?):(
         self.x = x or self.x;
         self.y = y or self.y;
         self.z = z or self.z;
      );
   );
}:(self :: template, x :: int, y :: int, z :: int):(
   self.x = x;
   self.y = y;
   self.z = z;
);
```

클래스 생성은 `new Point(인자들)` 으로 만들 수 있다.

/*
TODO: Class 를 제대로 또 클래스 안 객체들 말고도 그냥 이 클래스 자체에서도 함수 쓸 수 있게
```Tennin
@... 먼저 Point 라는 클래스를 만듬

@... 원래:
var playerXYZ :: Point = new Point(1,2,3);
playerXYZ.moveTo(playerXYZ.x + 1, playerXYZ.y + 1;);

@... 클래스 함수:
Point.allMove(1, 1) @... 예를 들어서 모든 이 Point 가 이만큼 움직이게 하는 함수
```
*/

PascalCase 를 사용한다.

> 컴파일러는 meta class 라는 기본 클래스 툴과 만든 클래스를 비교하여 형식상 제대로 된 클래스인지 검사한다.

### at

이 기획서를 보다보면 `at prototype ()` 같은게 정말 굉장히 많이 나온다. 그때마다 궁금했을지도 모른다. at 이 뭔지, at 은 간단히 인자 함수의 인자 부분에 `prototype = (...)` 하기에는 너무 가독성이 떨어지기 때문에 만든 함수다.\
근데 왜 `prototype = (...)` 의 가독성이 떨어졌다고 할까? python 도 사용하는 방식인데? 먼저 Tennin 는 = 같은 오퍼레이터 (연산자) 는 많이 사용하지 않고 애초에 최소화 한다. 하지만 `prototype = (...)` 은 naming 뒤에 = 라는 나라가 오는거긴 하지만\
흐름이 다 끊겨 버린다. 하지만 `at prototype ()` 은 흐름이 끊기지 않는다. 그렇기 때문에 읽을 때도 계속 잘 읽을 수 있다. (참고로 at 은 `... = (...)` 을 넣어주는 문법 설탕 함수 — 치환형 expand 다.)

### next, prev

일반적으로 JavaScript 나 C++ 같은 언어는 i 라는 변수에 1 을 더해라를 `i++` 이라고 한다. 하지만 이건 가독성이 좋지 않아서 Python 같은 가독성 중심 언어는 `i += 1` 이라고 해야한다. 하지만 이것도 좀 보기 불편하다.\
그래서 있는게 next, prev 함수다 먼저 next 는 무슨 변수에 1 을 더한다. 또한 그 더한 값을 return 한다.

예시:

```Tennin
var i :: int = 1;

Line.addLine(next i); @... 1 + 1 해서 i 는 2 가 되고 2 출력
```

그리고 비슷하게 prev 는 next 의 반대다 1 을 빼고 그 값을 return 한다.

```Tennin
var i :: int = 2;

Line.addLine(prev i); @... 2 - 1 은 1 이고 1 을 출력하니까 1 출력
```

### 출력

출력은 두 함수다: `Line.addLine` (범용) 과 `print` (숫자 전용).

print 는 이름 그대로 pr + int — 숫자만 출력한다. 말장난에서 시작했지만 남긴 이유는 진지하다: 두 출력 함수의 겹침을 기능 분화로 없앴고 (1-A #11 — 중복이 생긴 여지를 없앤다), zevra 의 이름을 남겨 설계자 시그니처를 유지한다.

정직한 기록: 숫자 출력에는 여전히 두 길이 있다 — #10 의 기록된 예외다 (이유: zevra 호환 + 개성).

zevra 호환은 부분이다: zevra 의 `print("문자")` 는 Tennin 에서 에러다.

TODO: print 의 숫자 범위 (int 만? double 포함?), 개행 여부.
(두 함수가 생긴 착오의 역사는 data.md 로.)

## job

job 이란 시작 기호 `(<` 닫는 기호 `>)` 으로 이 함수, 변수, 클래스, 이 구간 등이 무엇을 하는건지 정할 수 있다.\
만약 한 변수, 한 함수, 한 클래스만 가리키고 싶다면 `#` 을 쓰면 된다.

예를 들어 만약 언제나 이 변수는 참조를 하게 하고 싶으면

```Tennin
var x :: int = 1;

ref(<

var y :: int = x;
var z :: int = y;


@... 이 구간에 변수 선언은 참조

>);
var a :: int = x;
var b :: int = y; @... 그냥 값 복사

x = 5;

Line.addLine(x); @= 5
Line.addLine(y); @= 5
Line.addLine(z); @= 5
Line.addLine(a); @= 1
Line.addLine(b); @= 1
```

예시로 ref 라는 job 을 보여줬는데 그 외에 많은 job 이 있다.

또 # 를 쓰는법은

```Tennin
var x :: int = 1;

#ref
var y :: int = x

x = 2;

Line.addLine(y); @... 2
```

참고로 Tennin 의 if /\s/ then continue 원칙에 의해

```Tennin
var x :: int = 1;

# ref

var y :: int = x;

x = 2;

Line.addLine(y); @... 2
```

가 된다.

또한 어떤 job 안에 있는 것들은 그 job 이 아닌데 이게 뭔 소리냐면

```Tennin
#main
func main ():(
   var x :: int = 1; @... #main 이 여기까지 적용되면 안 된다.
):();
```

그래서

```Tennin
main(<
func main ():(
   var x :: int = 1;
):();
>);
```

이래도 main 의 영향력은 바로 안 main 까지만이고 x 에는 적용이 안 된다.

그 대신 구간을 나타내는 job 같은건 거기 안 까지도 다 적용이 되는데 이건 job 을 선언할 때 kind 를 나타낼 때 kind 가 JobKind.Interval 가 된다

job 과 인자 함수의 경계: **job 은 국·스코프·수명을 만들지 않는다. 만들면 그건 인자 함수다.** job 은 단순 이름표가 아니라 의미를 바꿀 수 있는 표식이다 (파이썬 데코레이터처럼 — ref 는 그 구간에서 `=` 의 동작을 바꾼다).

TODO: job 선언법 만들기

### 모듈

모듈은 간단히 다른 파일의 클래스, 함수, 변수 들을 얻을 수 있는것이다.

#### 모듈 만드는법

/*
TODO: 모듈 만드는법 정리하기 zevra 방식을 이어받아 id 방식으로 가기 또한 모듈 부분은 module job 으로 하기 그리고 예외 상황 처리 예를들어 id 가 없을 때는 이 모듈의 파일 이름 (확장자명 빼고) 로 되게 그런거
또 이 모듈을 take 했을 때 자동으로 실행되는 코드 부분을 표시하는 job 도 만들면 좋을것 같다.
*/

job 형식으로 모듈은 #export 또는 #module 이 좋을듯

#### 모듈 불러오는법

다른 모듈을 불러 오는 법은 다음과 같다.

```Tennin
take 모듈 아이디 import 가져올 함수나 클래스 이름 as 그 모듈을 사용할 때 사용할 이름;
```

여기서 import 랑 as 를 생략할 수 있다. as 를 생략하면 그 모듈의 id 가 자동으로 사용할 때 사용할 이름이 되고 import 가 없으면 그 파일의 모든 모듈이 가져와진다.

```Tennin
take MyWayArray;
```

또한 using 이라는게 있는데 대충 tenn 세팅 할 때 쓴다 자세한 내용은 바로 아랫줄 main 을 참고해라

### main

main 은 이게 메인 함수, 변수, 클래스 같은 거라고 말하는 job 이다.

간단히 말해서 java 는 class 안에 Main 이라는 함수로 이게 main 임을 표시한다. 하지만 Tennin 에선 그 대신 main 이라는 job 을 쓴다.

원래 전체 Tennin 코드는 이래야 한다.

```Tennin
using tenn;

#main
func helloWorld():(
   Line.addLine("Hello, World");
):();
```

참고로 using 으로 VarNumB 같은 언어도 사용 가능 하다. using vnb; 왜냐면 Var Num B 같은 Tennin 만들 때 만든 실험형 언어는 따로 그 형식 파일 그러니까 .vnb 가 Var Num Binary 파일 그런건 없다.\
그래서 그냥 그런 실험형 언어들도 Tennin 에서 그냥 사용 하는걸 가능하게 하는거다.

근데 사실 그게 진짜 이유면 그냥 이건 컨셉이다. 진짜 필요 없는 컨셉 사실 진짜 이유는 가독성 때문에 그렇다.

처음에 바로 시작하면

```Tennin
#main
func helloWorld():(
   Line.addLine("Hello, World!");
):();
```

뜸을 들이지도 않고 시작하기 때문에 처음부터 복잡하고 뭔가 보기 불편하다.

하지만 처음에 `using tenn;` 부터 시작하고 뜸을 들이면

```Tennin
using tenn;

#main
func helloWorld():(
   Line.addLine("Hello, World");
):();
```

처음부터 복잡하지 않고 처음에 간단한 선언문 부터 있으면 보기에 여기가 본문이구나가 바로 보이기 때문에 좀 더 읽기 좋다. ~~아님 말고~~

### Yulist

Tennin 의 모든 자료 구조는 **Yulist** 하나로 통일한다. 배열·연결 리스트·딕셔너리는 Yulist 가 아닌 특수한 자료가 아니라, **몇 가지 세팅이 이미 되어 있는 Yulist** 다 (프리셋). 사용자는 같은 재료로 새 자료 구조도 만들 수 있다 (1-A #9 — 유저와 시스템을 구분하지 않는다).

Yulist 는 표현 지향 기능이다. `mode = list` 같은 옵션 (스무고개) 이 아니라, **값을 찾을 때 뭘 할지, 수정할 때 뭘 할지, 생성될 때 뭘 할지를 표현한다.**

**정체**: 특별한 탐색 문법 `Yulist[?]` 하나를 빼면, Yulist 는 새 코어 메커니즘이 아니라 **Interception 과 expand 위에 지은 큰 라이브러리**다 (1-A #10 — 훅 메커니즘은 Interception 하나뿐이다).

**성능 — 표현은 expand 시점에 확정된다**: 표현 = 메모리에 실제로 놓이는 모양 (연속 배열 / 흩어진 노드 / 해시 버킷). 이 Yulist 가 어떤 구성 (프리셋 + 훅) 인지 expand 가 컴파일 타임에 읽고, 구성마다 그에 맞는 진짜 배치의 기계어를 굽는다. array 프리셋은 전개 후엔 그냥 연속 배열 인덱싱 기계어다 — 런타임에 "이건 무슨 종류지?" 를 묻는 코드가 한 줄도 안 남는다. (Lua 의 table 은 뭘 담든 속이 사실상 해시라 배열의 캐시 이점이 죽는다 — 그 함정을 피하는 문장이 이거다.) 실행 중에 구성이 바뀌는 Yulist 만 간접 비용을 내고, 그 비용은 그걸 고른 사람이 낸다 (비용 정직).

역할 분담: **컴파일러 몫 = 표현** (어떤 모양으로 굽나), **사용자 몫 = 배치** (그 모양들을 SM 어디에 모아 두나 — 값이 흩어지는 걸 막는 건 사용자의 몫이다).

**국적**: Yulist 에 값을 넣으면 그 값은 Yulist 의 국적으로 이주한다 (컨테이너 탈출 규칙과 정합 — → 메모리 관리).

**탐색**: `Yulist[?]` — 인덱스 자리를 condition 타입으로 선언하면 `bricks[name == "door" and color == "red"]` 같은 질의가 성립한다 (M.G.S 2.0 의 where 를 문법 추가 없이 흡수 — → 타입 매직).

**이름**: Yu 는 You 와 발음이 같다 — "너의 자료 형식". (C 가 B 다음이라 C 인 것처럼, 이름의 이야기는 한 줄이면 된다.)

TODO: 프리셋 목록 (array / linkedList / dictionary / set …) 과 각 프리셋의 훅 구성 명세, `Yulist[?]` 의 `?` 표기 확정, 등록부 패턴과 약한 참조 문제 (→ UNKNOWN.md).

### 메모리 관리

**목표 (포지셔닝)**: C·C++ 는 메모리 누수 방지가 사용자의 몫이고, Rust 는 안전하지만 어렵다. Tennin 는 **메모리 누수 방지와 안전을 컴파일러가 진다.** 그 대가로 참조를 저장·반환하는 일부 패턴을 금지한다 — 쉬움을 사는 값이다. (참고: 구조적 누수까지 막는 건 Rust 도 못 하는 일이다 — Rust 에서 Rc 순환·mem::forget 은 '안전한' 코드다. 단, 살아 있는 등록부가 객체를 계속 붙드는 논리적 누수는 어떤 언어도 못 막는다 — → UNKNOWN.md.)

**end with free** — 국이 끝나면 그 국의 값은 즉시 소멸한다 (국 = 아레나). 이것은 문법이다: 소멸 시점이 코드에 보인다. GC 처럼 실행을 멈추고 수색하지 않는다 — 해제 시점이 컴파일 타임에 확정된다.

**send = 자동 이주** — 국의 값이 국보다 오래 사는 합법 경로는 send (이주) 와 명시적 복사뿐이다.

- send 에 별도 표시는 없다. send 자체가 이주 명령이다.
- 보낸 뒤 원래 자리에서 재사용하면 컴파일 에러다: "x 는 N 행에서 send 로 이주했습니다."
- 기본은 이동 (포인터만 넘어감 — 비용 0). 복사는 명시한다 (문법 TODO).
- 캡처를 품은 값 (condition, 인자 함수) 은 send 시 캡처된 변수가 함께 이주한다 — 그 시점의 스냅샷이다. 이후 원본이 바뀌어도 반영되지 않는다.

**탈출은 컴파일 에러** — send·복사 없이 국의 값을 밖으로 내보내는 모든 길은 막힌다: 반환으로, 컨테이너에 저장으로, 클로저 캡처로, condition 캡처로. 각 판정 규칙은 그 자리만 보고 결정된다 (지역적·구문적). 전체 프로그램 분석이 필요해지면 그 규칙은 재설계한다 — 이것이 "Rust 보다 쉽지만 안전" 의 실측 기준이다.

**SM (Substitution Memories)** — Tennin 에서는 저수준으로도 코드를 짤 수 있다. 정확히는 SM 이라는 대체 메모리 위에서 주소에 값을 저장하고 그 주소에 있는 값을 얻는 것이다.

- **대체 원칙**: Tennin 는 위험한 것을 금지하지 않는다 — 대체한다. VMware 가 가짜 CPU 를 주듯, SM 이 가짜 메모리를 준다. 사용자에게는 대체물만 제공되고, 실제 메모리는 언어만 만진다.
- SM 은 작은 놀이터가 아니라 **프로그램 전체의 대체 메모리 공간**이다 — var 도 Yulist 도 전부 SM 위에 산다. 층위: SM (전체) ⊃ 국 아레나들 (구획) ⊃ 값들.
- `SM.store(주소, 값)` — 어떤 메모리 주소에 이 값을 넣기 / `SM.load(주소)` — 어떤 메모리 주소에 있는 값을 얻기. (LLVM·하드웨어와 같은 용어. 인자 순서는 TODO.)
- **일반 var 접근에는 범위 검사가 0 개다** — 주소를 컴파일러가 배치했으니 틀릴 수가 없다. 검사가 붙는 건 사용자가 주소를 손으로 만드는 SM.store / SM.load 뿐이다.
- 범위 검사 2단: 컴파일 타임에 판정 가능하면 (리터럴 등) 컴파일 에러. 나머지는 가드 페이지 — 범위 밖 접근은 하드웨어가 트랩으로 잡는다 (검사 코드 0 개, WASM 런타임 방식). 임베디드·32비트 폴백은 if 검사 — TODO.
- **포기 (기록)**: 커널·드라이버급 실메모리 직접 제어는 Tennin 의 도메인이 아니다. 그 대가로 unsafe 없는 언어를 얻는다. C FFI 는 SM ↔ 실메모리 복사 계층 (마셜링) — TODO.

TODO: 복사 명시 문법, 참조/빌림 규칙 (func·큐 문법과 함께), 탈출 각각의 최소 코드 + 에러 문안.



## expand

expand 는 특수 함수다 (var·const 처럼 사용자가 만들 수 없는 함수).

expand X 는 "X 를 컴파일 타임에 아는 만큼 실행하고, 모르는 만큼 그 자리에 전개하라"다.

- 전부 알면 → 값만 남는다 (comptime/상수계산)
- 아무것도 모르면 → 본문이 통째로 그 자리에 붙는다 (인라인)
- 표기를 조작하는 함수면 → 치환 (at 이 이 종류다 — `... = (...)` 을 넣어주는 문법 설탕)

하나의 의미론에 결과가 셋이다 — 1-A #10 (중복 금지) 의 실현이다. (이런 방식을 부분 평가라고 부른다. Zig 의 comptime 이 같은 계보다.)

전개는 실패하지 않는다 — 모르는 것은 에러가 아니라 남는 코드다. 유일한 에러는 전개 한도 초과다. 깊이를 모르는 재귀는 복붙이 끝나지 않기 때문이다 (어떤 컴파일 타임 시스템도 못 피한다 — Zig 도 quota 로 막는다). 한도는 expand 가 인자로 받고 기본값은 2000 이다.

다메커니즘 함수 (if, while …) 는 expand 로 정의된다. 그래서 제어구조의 호출 비용이 0 이다.

대외 설명 첫 줄: expand 는 텍스트가 아니라 의미 단위로 동작한다. C 식 텍스트 매크로가 아니다.

TODO: 전개 한도의 단위 (전개 1회 = 무엇을 세나), 정의-expand 와 호출-expand 의 표기 — func 문법 확정 때.



### Interception

//TODO: proxy 느낌의 이거 대신 이거 해주세요 하는 기능 setter 그런거 합친거 참고로 이건 proxy 처럼 다 가져오는게 아니라 딱 설정한 부분만 예시로 set 하면 set 할 때만 = 로 set 할 때만

### kind

먼저 kind 는 enum 이다 그 대신 종류 느낌?

먼저 kind 는 enum 처럼 enum.A = 1, enum.B = 2 이런게 아니라 그냥 kind.A 는 kind.A 라는 객체고 kind.B 는 kind.B 라는 객체다. 어떤 식으로 구현이 되냐면

원래 kind.A 는 0 이다, kind.B 는 1 이다. 그 대신 타입이 그 kind 다

```Tennin
kind Keyword (
   VAR; CONST; LET
);
```

이때 Keyword.VAR 는 0 이라는 값이 저장된다. 그 대신 타입은 Keyword 다. 그리고 이때 타입변환 까지 하는 비교 ~= 로 0 과 Keyword.VAR 을 비교하면 재미있는 일이 일어난다.\
~= 는 먼저 Keyword.VAR 의 데이터를 뽑아온다. 타입은 뽑아오지 않고, Keyword.VAR 의 데이터는 0 이다, 또 0 의 타입 int 도 빼오지 않고 값 0 만 빼온다. 그러면 0 == 0 이 된다. 결과는 true 다.

kind 선언문은 위에서 봤겠지만

```Tennin
kind 종류 이름 (
   종류 이름 1; 종류 이름 2; ...
);
```

이런 식이다. 예시로

```Tennin
kind Keyword (
   VAR; CONST; LET
);
```

이런 식으로 Tennin 로 언어를 만들 때 kind 를 사용할 수 있다.

또 원래 있는 kind 에 새걸 추가할 수도 있다.

```Tennin
kind FuncKind (
   DO_RETURN; LOOP; IF; INLINE @... TODO: INLINE 을 EXPAND 로 개명할지 검토 (expand 결정과 통일)
);
```

여기서 사용자가 새 값을 추가하려면

```Tennin
FuncKind.add(새 종류 이름, index);
```

먼저 여기서 새 종류 이름은 여기에 무슨 이름의 종류를 추가할까고 index 는 이걸 int 로 타입 변환 했을 때 무슨 값이 나오냐며 기본값은 맨 마지막 종류의 index + 1 이다.

예시로

```Tennin
FuncKind.add(FUNC); @... 지금은 가장 마지막 인덱스가 3 이니까 index 는 자동으로 4
```

참고로 index 매개변수는 언제 넣냐면 이 kind 에서 이 두 종류는 같은 걸로 나타내고 싶을 때 쓴다. 그러니까 FuncKind 중 뭐 LOOP 랑 MY_LOOP 랑 같게 하고 싶을 때 index 를 사용하면 된다.\
사실 거의 필요 없는거여서 안쓰는걸 권장하고 필요할 때 진짜 완전 필요할 때만 쓰는걸 권장한다.

참고로 왜 갑자기 FuncKind 라는게 생기는지 궁금할 수 있는데 kind 함수는 expand 로 FuncKind 타입을 선언하고 거기 안에 메소드를 추가하는 등을 한다.

kind 의 규칙:

- kind 의 index 는 0 부터 시작한다.
- kind 비교는 `==` 를 쓴다. (`~=` 예시는 이해용이다. 참고: 서로 다른 kind 라도 index 가 같으면 `~=` 는 true 다 — `~=` 의 일관된 귀결이니 함정 주의.)
- add 는 expand 시점 (컴파일 타임) 에 실행된다. 사용자 소스에서의 추가는 자유다 (예: 나라 운영 게임에서 세금 계산·반란 확률 같은 함수들을 분류하는 `FuncKind.add(COUNTRY_FUNC)`). 프로그램 실행 중 추가는 불가 — break/continue 의 level 규칙이 kind 를 읽기 때문이다.
- 같은 이름을 add 하면 컴파일 에러다 (1-A #11).
- 새 kind 에는 기본 규칙이 붙지 않는다 — 그 kind 를 읽는 쪽이 의미를 준다.

### 네이밍 컨벤션

// TODO: 네이밍 컨벤션 다 정하고 통일하기

## 1-D. 실행 모델 — Stack / Sub-stack / Queue (동시성)

Tennin의 동시성·실행 모델은 **협력적 스케줄링 기반의 계층형 액터 시스템**이다. 공유 메모리가 아니라 **메시지 전달**을 기본으로 한다. (설계자의 브라우저 zevring 런타임에서 실행 모델만 가져왔다 — 브라우저·렌더링 부분은 뺐다.) 이 모델이 Tennin의 정직한 비용 노출·자유도·네이티브 성능과 맞물린다. 아래는 이 문서만 읽어도 실행 모델 전체를 알 수 있게 세부까지 적는다. (**큐·스택 선언 문법과 메시지 API의 정확한 표기는 1-C 문법의 큐/스택 부분에서 정의한다** — 여기서는 실행 모델·개념만 적는다.)

### 세 단위 — 감독 / 코치 / 선수

- **Stack** — 최상위 스케줄러. 실행 단위(프로그램)당 **정확히 하나**. 큐들의 실행을 고르고 지시하는 얇은 루프다(무거운 계산은 안 함). (비유: 뇌·감독)
- **Sub-stack** — 위임 스케줄러. Stack의 명령을 받는 점은 큐와 같지만, 일반 큐와 달리 (1) **독자 실행이 가능**하고 (2) **다른 큐를 통제**한다. (Erlang/OTP supervision tree 계보. 비유: 코치)
- **Queue(큐)** — Stack/Sub-stack 지시 아래 움직이는 실행 단위(액터). 각자 FIFO 메일박스를 갖고 **메시지로만** 통신. (비유: 손발·선수)

- **왜:** 스케줄러 하나가 모든 큐를 직접 통제하면 규모가 커질수록 병목·관리부담이 된다. 감독–코치–선수 계층으로 통제를 위임해 감독을 얇게 유지한다.
- **장점:** 확장에 강하고, 실패한 단위를 감독자가 재시작하는 검증된 패턴(supervision).
- **단점:** 단위가 셋이라 배울 개념이 늘고, 큐 소유권(트리) 규칙을 명확히 정해야 한다(→ TODO).

### 큐(Queue) 상태 머신

| 상태 | 의미 |
|------|------|
| `waiting` | CPU를 기다림. ready 큐에 있으며 스케줄러가 다음 실행 대상으로 **선택 가능** |
| `running` | 현재 실행 중. **동시에 하나의 큐만** 이 상태 |
| `blocked` | 특정 이벤트(메시지 응답, 완료 등)를 기다림. 스케줄러가 **실행 대상으로 안 고름** |
| `complete` | 본문 실행 완료. 라이브 메모리 해제 + 압축 보관(동면) |
| `kill` | 강제 정지된 상태 |
| `failed` | 에러로 정지된 상태. 강제 정지(`kill`)와 **구분되는 별도 상태** |

```
(큐 생성) ─▶ waiting ──(선택)──▶ running ──┬─(양보)────────▶ waiting
              ▲                            ├─(메시지/대기)──▶ blocked ──(답 도착)──▶ waiting
              │                            ├─(본문 끝)─────▶ complete ──┐
           (부활)                          ├─(미처리 에러)──▶ failed
              │                            └─(강제 정지)────▶ kill
              └────────(complete 에 메시지 오면 부활)◀────────┘
```

- **`waiting`(ready)과 `blocked`를 반드시 구분하는 이유:** 구분 안 하면 스케줄러가 아직 답이 안 온 큐를 계속 골라, 실행해도 아무 일 못 하는 큐에 CPU를 낭비한다. blocked 큐는 기다리던 답이 도착해야만 waiting으로 복귀해 선택 대상이 된다.
- **`failed`를 `kill`과 나누는 이유:** 강제 정지(외부 의지)와 에러 정지(내부 사고)는 원인이 달라 감독자의 대응도 달라야 하므로, 상태로 구분해야 판단이 된다.

### 스케줄러 실행 루프

```
Scheduler.run():
   while (ready 에 큐 있음):
      q = ready.shift()               // 자기 순서대로
      q.state = running; current = q
      q 를 "다음 양보 / blocked / complete 까지" 실행
      - 양보 지점 도달 & 다른 ready 있음 → q 를 waiting 으로 park
      - 메시지/응답 대기 → q 를 blocked 로
      - 본문 끝 → q 를 complete 로
```

**중요 — 병렬이 아니라 인터리빙이다.** "A가 돌다가 B의 요청이 오면 B 실행"은, A가 양보하고 스케줄러가 ready가 된 B를 고르는 방식이다. 병렬처럼 보이지만 실제로는 하나의 흐름이 교차하는 인터리빙이며, 데이터 경쟁이 없고 추론·디버깅이 단순하다(의도된 기본값). 진짜 병렬은 워커 승격(아래)으로 별도 경로.

### 자동 양보 안전망 (기아 방지)

순수 협력 스케줄링은 양보 안 하는 큐 하나가 전체를 멈춘다(기아). 그래서 안전망을 이중으로 깐다.

- **명시적 `tick`** — 협력용. 큐가 자발적으로 양보.
- **자동 양보** — **컴파일러가 루프 백엣지·함수 호출 경계에 양보 체크(카운터 감소, 0이면 강제 양보)를 삽입**한다. (zevring은 인터프리터가 reduction을 세지만, Tennin은 네이티브 AOT라 인터프리터가 없으므로 **컴파일러가 체크 명령을 심는 방식**으로 같은 보장 — Go의 협력적 선점 계열.)

- **삽입 지점을 한정하는 이유:** 협력 스케줄링의 숨은 계약은 "양보 전엔 아무도 안 끼어든다"는 원자성이다. 임의 지점 삽입은 이걸 깬다(읽고-수정하고-쓰는 구간 등). 그래서 삽입은 **루프 백엣지·호출 경계로만** 한정하고, 절대 안 끊겨야 하는 구간은 **`atomic { ... }`** 로 자동 양보를 억제한다. (원칙 3, 숨은 제어흐름 없음과 같은 정신.)
- **경고 정책:** 양보 지점 없는 루프는 에러가 아니라 **린트 경고**다(안전망이 있으니 버그가 아니라 "지연 예측이 안 되는 코드"). 단 **정적으로 unbounded인 루프에 양보 지점이 없을 때만** 경고 — 모든 tick 없는 루프에 경고하면 소음이 되어 진짜 경고를 묻는다.

### Sub-stack — 위임 스케줄러 (코치)

Stack 하나가 모든 큐를 직접 통제하는 부담을 계층으로 나눈다. Stack(감독)은 Sub-stack(코치)에게 큰 지시만 내리고, 개별 큐 통제는 담당 Sub-stack이 대행한다.

| Sub-stack 상태 | 의미 |
|------|------|
| `running` | 자기 본문 코드 실행 중 |
| `waiting` | 기다리는 중 |
| `stacker` | 메인 Stack의 일 일부(담당 큐들 통제 등) 수행 중 — **Sub-stack 고유 상태** |
| `kill` | 메인 Stack에 의해 종료. **단, 일반 큐와 달리 재시작 가능(rebirth)** |
| `complete` | 완료 |
| `failed` | 에러남 |

- **`stacker` 상태:** 자기 본문 실행(`running`)과 코치 업무(담당 큐 통제)를 별도 상태로 나눠, 프로파일링·디버깅에서 "자기 일 하는 시간"과 "통제 업무 시간"을 분리 관찰한다.
- **`kill` 차이 (핵심):** 일반 큐의 `kill`은 **재시작 불가**. Sub-stack의 `kill`은 **rebirth로 재시작 가능**.
- **워커 승격과의 관계:** Sub-stack + 담당 큐 그룹은 워커 승격의 자연스러운 단위 후보. 그룹을 통째로 스레드에 올리면 그룹 내부는 참조 전달을 유지하고, 그룹 경계를 넘는 메시지만 직렬화한다.

### 메시징 (액터 통신)

**직접 전달, 확실한 전달.** 전역 이벤트/브로드캐스트가 **아니다** — 특정 큐에 직접 전달한다.
- *전역/브로드캐스트 기각 이유:* 수신자 불명확·전달 보장 불가. "메시지는 **절대 버려지지 않는다**"는 원칙은 수신자가 특정된 직접 전달에서만 성립.

**메일박스.** 각 큐는 **FIFO 메일박스**를 가진다. `send`/`ask`로 온 메시지가 쌓이고, 버려지지 않는다. 메모리 위해 bounded 권장, 초과 시 **백프레셔**(발신자를 늦춤) 또는 에러.

**수신 — 두 스타일.**

| 방식 | 성격 |
|------|------|
| **on** (선언형 핸들러) | 메시지 도착 시 자동 디스패치 |
| **receive** (명령형) | 다음 메시지 올 때까지 `blocked` 대기. 순차 프로토콜용 |

**발신 — 두 스타일.**

| 방식 | 성격 |
|------|------|
| **send** | fire-and-forget. 응답 안 기다림 |
| **ask** | 요청-응답. 응답값으로 완료되는 Promise 반환 |

- **성능:** 인메모리 **참조 전달**이라 직렬화 없이 빠름. 직렬화는 워커로 승격된 큐에만.
- **데드락 방지:** (1) `ask` 기본 타임아웃(옵션으로 변경·무한 가능), 초과 시 reject. (2) 큐가 `blocked`로 갈 때마다 **대기 그래프 사이클 검출** — 사이클이면 즉시 `DeadlockError`로 reject("침묵 정지" 대신 "즉시 명확한 에러"). (3) 가득 찬 메일박스에 send해 막힌 발신자도 같은 그래프에 등록 → send-send 데드락까지 하나로 검출. 단일 스케줄러 인메모리라 검사 비용 ≈ 0.

### 동면 (complete = 압축 보관)

큐가 `complete`되면 더 안 돌므로, 라이브 환경(변수·클로저)을 **해제하고 압축 보관**한다(메모리·성능). 다시 필요하면 **부활**한다 (예: complete 큐에 `send`가 오면 자동 부활 → 무유실 원칙이 동면 중에도 유지).

- **부활 1 — one-shot:** 큐의 함수와 그 인자(props)만 남기고 환경을 버린다. 부활 시 그 함수를 인자로 재호출. **직렬화 불필요, 가장 저렴.**
- **부활 2 — 스테이트풀 스냅샷:** 상태를 직렬화·압축(스냅샷)한 뒤 환경을 버린다. 부활 시 풀어서 **이어서** 실행. DB·랜덤 써도 안전. **제약:** 함수·클로저는 직렬화 불가라, 동면 가능한 큐의 상태는 "**데이터만**"으로 구성돼야 한다.
- **❌ 리플레이(이벤트 소싱) 기각:** 메시지 로그로 처음부터 재생하는 방식은 안 쓴다. 부활 비용 O(역사)라 큐가 오래 살수록 느려지고, DB·랜덤·시간 쓰면 재생 비결정성(위험). 순수 액터 한정 옵션.
- **`data` 큐 계약 (일석이조):** 스냅샷 동면 조건("상태는 데이터만")이 워커 승격의 직렬화 조건과 **정확히 일치**한다. 그래서 큐 선언 시 플래그 하나로 (a) 스냅샷 동면 가능 + (b) 워커 승격 가능 + (c) 상태에 함수/클로저 넣으면 스냅샷 시점에 에러 를 **하나의 계약**으로 묶는다. 개발 모드에서 주기적으로 직렬화 가능성을 검사(배포 후 "동면하려니 안 됨" 방지). 클로저가 필요한 큐는 계약을 선언 안 하면 됨(one-shot 사용) — **강제가 아닌 선택 계약**.

### 워커 승격 (진짜 병렬)

CPU 무거운 큐를 **실제 OS 스레드**에 올려 멀티코어 병렬을 얻는다.

- **"무료 업그레이드" 논증:** 액터는 처음부터 공유 메모리 없이 메시지만 주고받으므로, 큐를 스레드로 옮겨도 코드가 안 깨진다. 공유 메모리 방식이었다면 스레드 간 데이터 분리가 불가능했을 것(공유 메모리 동시성 기각의 근본 이유). → 지금 액터로 짜두면 병렬화가 **코드 수정 없는 무료 업그레이드**.
- **비용·정책:** 워커는 메모리 공유 안 하므로 메시지를 **직렬화**해 보낸다. 그래서 기본은 인메모리(참조) 유지, CPU 무거운 큐만 승격한다. (전면 워커화 기각: 대부분 큐는 가볍고, 모든 메시지에 직렬화 비용을 부과.) 승격 자격은 위 `data` 큐 계약으로 판정.

### 깊은 재귀 & 일급 연속 (런타임 코어)

깊은 재귀는 OS 콜 스택을 넘겨 터진다. 그리고 **큐/스레드로는 못 푼다** — 스레드도 스택 한계가 같고, 깊은 재귀는 호출 N이 N+1의 결과에 의존하는 **끊기지 않는 하나의 사슬**이라 여러 큐로 못 쪼갠다. 근본 해법은 재귀를 반복 + 명시적 스택으로 바꾸는 것(트램폴린 / CPS).

- Tennin 런타임은 큐의 실행 스택·연속을 **힙에 명시적으로** 둔다(스택리스/성장형 스택). 효과:
  - **(a) 선점이 공짜** — 실행 루프 아무 지점에서 멈출 수 있어, 위 자동 양보가 "카운터 확인 한 줄"이 된다.
  - **(b) 깊은 재귀·suspend가 OS 스택에 안 갇힘** — 큐가 값싸게 `blocked`↔`waiting`.
  - **(c) 연속이 일급 데이터** — 제너레이터/async, "실행 중간 스냅샷"의 길이 열린다.
- **일석이조:** 큐 suspend/resume과 제어 층의 `yield`(소국)가 **같은 일급 연속** 위에 선다. 동시성과 제어가 별도 기계가 아니라 하나의 바닥을 공유한다 (→ data.md #7의 그 바닥).

> 미정(큐 소유권 트리 규칙·`failed` 감독 프로토콜·`data` 큐 계약 표기·`atomic`/`tick` 문법·양보 체크 삽입 지점과 카운터 초기값 등)은 TODO.md 참고.

## 1-E. bash

먼저 tennin 파일 실행하는 법이다.

```bash
tenn <파일 이름>
```

이다 만약에 결과물을 남기고 싶으면

```bash
tenn <파일 이름> -o <실행 파일 이름>
```

이렇게 해서 결과물을 남길수 있다.

예시로 WorldWar.tenn 이라는 게임 파일을 실행하고 싶으면

```bash
tenn WorldWar.tenn
```

이라고 하면 되고 만약 특정 경로에 있는걸 실행하고 싶다면

```bash
tenn "C:\WorldWar\START.tenn"
```

이렇게 하면 된다.

## 2. 컴파일 파이프라인

```
소스코드
   │
   ▼
[Lexer]    문자열 → 토큰 스트림
   │
   ▼
[Parser]   토큰 → 추상 구문 트리(AST)
   │
   ▼
[CodeGen]  AST → LLVM IR
   │
   ▼
LLVM       최적화 + 기계어 생성 + 실행파일
```

앞단(Lexer, Parser, CodeGen)만 직접 구현하고, 최적화와 기계어 생성이라는
가장 어려운 부분은 LLVM이 담당한다.

## 3. 설계 원칙: Deep Module

각 컴파일 단계를 "좁은 인터페이스, 깊은 구현"의 deep module로 만든다.
복잡성을 모듈 내부에 가두어 다른 부분에 새어 나가지 않게 한다.

| 모듈    | 공개 인터페이스              | 내부에 숨기는 것                     |
|---------|-----------------------------|-------------------------------------|
| Lexer   | `tokenize(source) → tokens` | 문자 처리, 공백 스킵, 숫자/문자열 파싱 |
| Parser  | `parse(tokens) → AST`       | 문법 규칙, 연산자 우선순위, 에러 복구  |
| CodeGen | `generate(ast) → LLVM IR`   | IRBuilder 조작, 타입 매핑            |

인터페이스가 안정적이므로, 나중에 내부 구현(예: 파싱 알고리즘, IR 생성 전략)을
바꿔도 다른 모듈은 영향을 받지 않는다.

**정직한 Deep Module**: 좁은 인터페이스·깊은 구현은 유지하되, **알고리즘 복잡도(내부 구현의 복잡함)는 감추더라도 비용은 감추지 않는다**. 할당이 일어나는지, 에러가 날 수 있는지, 제어가 어디로 이전되는지(할당·에러·제어 이전)는 인터페이스에 드러낸다. 비용을 숨기면 사용자가 성능을 예측할 수 없어 원칙 1-A #1(성능 최우선)과 충돌하기 때문이다. 즉 "복잡함은 숨기되, 값비쌈은 숨기지 않는다".

## 3-A. 기획

(`CONTRIBUTING.md` 에 나와있는 내용)

(원본은 `CONTRIBUTING.md` — 여기는 요약이다.)

- 모든 기록은 까먹을 수 있다. 그러므로 세세한 것까지 `tennin-spec.md` 에 저장한다. 하나가 빠지는 순간 완성도가 낮아지고 지적할 부분이 많아진다.
- "왜"가 가장 중요하다. 특히 다른 사람이 반박할 만한 부분에는 반드시 왜 그렇게 했는지 적는다. 처음 보는 사람이 납득할 수 있어야 한다.
- 특별한 부분 (장단점이 확실히 구분되거나, 두 마리 토끼를 잡거나, 무언가를 포기했을 때) 은 실재할 때만 적는다. 단점을 적자마자 우회로로 해소하지 않는다.
- 이유에는 네 종류가 있다: 제약 / 도메인 / 고통 / 철학. 철학 하나로만 서 있는 결정은 재검토 후보다.
- 딴 일 하다 떠오른 것은 `IDEAS.md`, 모르는 것은 `UNKNOWN.md`, 미룬 것은 `TODO.md`, 바뀐 것은 `CHANGELOG.md` (날짜 / 전 → 후 / 왜 / 무엇을 버렸나) 에 적는다.
- 우리가 만든 것을 비판적으로 생각해 본다. 결정은 사람이 한다.

## 4. 구현 아키텍처 (C++)

- **Token / Lexer:** `enum class TokenType`, `struct Token`
- **AST:** 노드 클래스 계층 + 방문자(Visitor) 패턴
- **CodeGen:** LLVM `IRBuilder`로 IR 조립 (예: `2 + 3` → `Builder.CreateAdd(...)`)
- **빌드 시스템:** CMake, LLVM 개발 라이브러리 링크, C++17 이상

참고: LLVM 공식 "Kaleidoscope" 튜토리얼이 이 경로(간단한 언어 → LLVM 네이티브
컴파일)를 단계별로 다루므로 뼈대 참고용으로 활용.

# Core

Core: Tennin의 목적은 사용자가 짜는 코드의 구현 복잡도를 낮추는 것이다.\
자유도(다메커니즘 함수, Feature Absorption)는 그 목적을 위한 수단이다 — 복잡도를 낮추지 못하는 자유는 넣지 않는다.\
제약은 둘: 네이티브 성능과 메모리 안전은 깨지 않는다.\
이 단순함의 대가는 언어 구현과 학습 곡선이 지불한다.

-- *tenn*
